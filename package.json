{
  "name": "dukkanci",
  "version": "1.0.0",
  "private": true,
  "description": "منصة دكانجي للطلب من المتاجر المحلية",
  "scripts": {
    "start": "node server.js",
    "check:versions": "node scripts/check-versions.js",
    "build:catalog": "node scripts/build-shared-catalog.js",
    "build:catalog:dry": "cross-env DRY_RUN=true node scripts/build-shared-catalog.js"
  },
  "dependencies": {
    "sharp": "^0.33.5"
  },
  "devDependencies": {
    "cross-env": "^7.0.3"
  }
}
