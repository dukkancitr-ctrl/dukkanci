# CLAUDE.md — ذاكرة مشروع دكانجي

**قاعدة إلزامية لأي جلسة عمل (بشرية أو ذكاء اصطناعي):**
1. اقرأ هذا الملف كاملاً قبل البدء بأي مهمة — خصوصاً قسم "الأخطاء المتكررة المعروفة" وقسم "سجل الإصلاحات" في الأسفل، للتأكد أن ما تحاول حلّه لم يُحلّ سابقاً بطريقة معينة يجب اتّباعها أو تجنّب تكرارها.
2. لا تُغلق أي مهمة (إصلاح أو ميزة جديدة) قبل إضافة سطر جديد في "سجل الإصلاحات" أسفل هذا الملف. هذا الملف هو الذاكرة الوحيدة الدائمة للمشروع بين الجلسات المختلفة.

---

## 1. نظرة عامة سريعة

- **المنصة:** دكانجي — سوق طلبات محلي (مطاعم وسوبرماركت) في تركيا.
- **الواجهة الأمامية:** `index.html` + ملف واحد `app.js` (~12,884 سطراً، 684 دالة) + `styles.css`.
- **الخلفية:** Vercel serverless functions تحت `api/` + قاعدة بيانات وتخزين ملفات على Supabase.
- **التطبيقات الجوالة:** عبر Capacitor (`android/`, `ios/`).
- **Service Worker:** `sw.js` — تخزين مؤقت + إشعارات Push.

## 2. مصدر الحقيقة الوحيد للبيانات — مهم جداً

- بيانات المتاجر والمنتجات الفعلية تُقرأ **حصرياً من Supabase**. مجلد `migrations/` يوثّق كل تغيير على المخطط بالترتيب الزمني (اسم الملف).
- ملفات `*-data.js` في جذر المشروع (56 ملفاً، مثل `zaitoune-data.js`) هي **كود قديم ميت (dead code)** — تأكدنا أن لا شيء في `app.js` أو `server.js` يستدعيها حالياً. **لا تُعدَّل ولا تُعتمد كمصدر بيانات أبداً.**
- عند التعامل معها نهائياً: انقلها إلى مجلد `archive/` أو احذفها، ونظّف مصفوفة `APP_SHELL` في `sw.js` من كل إشارة إليها (لا تزال تحتوي حالياً على ~30 إشارة قديمة لها).
- أي خطأ متعلق بظهور/اختفاء متجر → تحقّق أولاً من سجلّه في Supabase، وليس من هذه الملفات القديمة.

## 3. قواعد العمل بـ Git (إلزامية)

1. لا تترك أي فرع (branch) أو نسخة عمل (`git worktree`) مفتوحة لأكثر من يوم عمل واحد. كل ميزة/إصلاح: فرع قصير العمر → commit → دمج في `main` فوراً → حذف الفرع محلياً وبعيداً.
2. قبل البدء بأي عمل جديد:
   ```
   git checkout main
   git pull
   git status   # يجب أن يكون نظيفاً؛ إن لم يكن، توقف واسأل قبل المتابعة
   ```
3. عند الانتهاء من أي مهمة:
   ```
   git worktree remove <path>
   git branch -d <branch-name>
   git push origin --delete <branch-name>
   ```
4. لا تدمج فرعاً عمره أكثر من أسبوعين دون مراجعة يدوية كاملة لمحتواه — قد يعيد كوداً تجاوزه الزمن ويُلغي إصلاحات لاحقة.
5. إذا ظهر خطأ "Unable to unlink .git/index.lock"، فهذا يعني وجود عملية git عالقة أو متزامنة؛ تحقق من ذلك قبل حذف القفل يدوياً.
6. **ملاحظة حالة معروفة (2026-07-10):** المشروع كان يعمل على فرع `supermarket-baseline-categories` (وليس `main`) مع 111 ملفاً معدَّلاً وغير محفوظ. راجع حالة هذا الفرع قبل بدء أي عمل جديد ولا تتراكم عليه المزيد من التعديلات غير المحفوظة.
7. **تنظيف دوري:** الجهاز يستخدم PowerShell (لا يوجد bash افتراضياً)، لذا شغّل `.\scripts\git-cleanup.ps1` من PowerShell في جذر المشروع كل أسبوع تقريباً لحذف نُسخ العمل والفروع المدموجة تلقائياً (يوجد أيضاً `scripts/git-cleanup.sh` لمن يفضّل Git Bash). راجع يدوياً القائمة التي يطبعها السكربت في النهاية قبل حذف أي فرع منها.

## 4. قائمة تحقق النشر الإلزامية (Deploy Checklist)

عند تعديل `app.js` أو `styles.css`، رقم الإصدار `?v=NNN` يجب تحديثه معاً في **3 مواضع**:

1. `index.html` → وسم `<script src="/app.js?v=NNN">` ووسم `<link href="/styles.css?v=NNN">`.
2. `sw.js` → الثابت `CACHE = "dukkanci-vNNN"`.
3. `sw.js` → كل إشارة لـ `app.js?v=` و `styles.css?v=` داخل مصفوفة `APP_SHELL`.

**قبل كل نشر، شغّل:**
```
npm run check:versions
```
هذا السكربت (`scripts/check-versions.js`) يفشل صراحةً إن كانت الأرقام غير متطابقة بين `index.html` و`sw.js`، بدل اكتشاف الخطأ بعد وصوله للمستخدمين.

> **حالة معروفة عند كتابة هذا الملف:** `index.html` على `app.js?v=264` / `styles.css?v=161`، بينما `sw.js` لا يزال على `app.js?v=176` / `styles.css?v=111`. يجب تصحيح هذا الانحراف في أقرب نشر قادم.

## 5. خريطة الملفات الأساسية

| المسار | الدور |
|---|---|
| `index.html` | الصفحة الرئيسية وكل وسوم `<script>`/`<link>` |
| `app.js` | كل منطق الواجهة (عربة، دفع، عرض متاجر، لوحة تاجر، تتبع...) — ملف ضخم، **ابحث عن الدالة بالاسم قبل إضافة أي منطق جديد** لتجنّب التكرار |
| `styles.css` | كل الأنماط |
| `sw.js` | Service Worker: تخزين مؤقت + إشعارات Push |
| `api/` | دوال Vercel serverless (طلبات، دفع، خرائط، حملات...) |
| `lib/` | أدوات مشتركة (بوابة AI، معالجة صور الكتالوج، Google indexing...) |
| `migrations/` | كل تغييرات مخطط Supabase، مرتبة بالتاريخ في اسم الملف |
| `scripts/` | سكربتات بناء/استخراج بيانات لمرة واحدة لكل متجر — **ليست** جزءاً من التشغيل الحي |
| `catalog/` | الكتالوج المشترك بين متاجر السوبرماركت |
| `*-data.js` (جذر المشروع) | **كود قديم ميت — لا تستخدمه** (راجع قسم 2) |

## 6. الأخطاء المتكررة المعروفة (راجعها قبل إعادة الاختراع)

- **تجمد زر إرسال الطلب / رسوم توصيل صفرية** للمتاجر بدون إحداثيات — أُصلح منطقياً في `app.js`، لكن ظهور الإصلاح فعلياً يتطلب تطبيق قسم 4 (رفع رقم الإصدار في الأماكن الثلاثة) وإلا يبدو "غير مطبَّق" رغم أن الكود صحيح.
- **ظهور/اختفاء متجر** بعد إضافته مباشرة إلى Supabase — تأكد أن منطق العرض في `app.js` يقرأ نفس الحقول التي يُدخلها Supabase، وليس من `*-data.js`.
- **فقدان طلبات صامت** — عولج جزئياً عبر مسار حفظ مستقل في `api/notify-order.js` بدل الاعتماد فقط على كتابة المتصفح المباشرة. أي تعديل مستقبلي على تدفق الطلب يجب أن يمر عبر هذا المسار أيضاً وليس فقط الكتابة المباشرة.
- **ثغرات API غير محمية** — تحقّق دائماً من التحقق من الهوية/الصلاحيات عند إضافة أي `endpoint` جديد داخل `api/`.
- **حذف متجر من Supabase لا يكفي وحده** — كل متاجر السوبرماركت/المطاعم القديمة مبنية كملف `<slug>-data.js` مُحمَّل عبر `<script>` مباشر في `index.html` ومُدرَج أيضاً في `sw.js` (مصفوفة `APP_SHELL`)، وليس مجرد كود ميت كما هو الحال مع الـ56 ملف `*-data.js` المذكورة في قسم 2. حذف صف المتجر من Supabase فقط (بدون حذف `<slug>-data.js` وإزالة أسطره الثلاثة من `app.js`: `stores.push(...)`, `products.push(...)`, `...XDeliverySettings`) يترك المتجر ظاهراً على الموقع فعلياً رغم اختفائه من قاعدة البيانات — هذا بالضبط ما حدث مع متجر حلاب 1881 (id 49) بعد إزالته من Supabase في 2026-07-10 دون تنظيف الحزمة الأمامية.
- **تعديل حقل واحد (lat/lng/mapUrl/hours...) لمتجر "مدموج يدوياً" لا يكفي من Supabase وحده** — نفس فئة الخطأ أعلاه لكن على مستوى حقل واحد بدل المتجر كاملاً: بعض الحقول (`location`/`mapUrl` مؤكَّد، وربما غيرها) تُقرأ من `<slug>-data.js` الثابت حصرياً حتى لو كان للمتجر أيضاً صفّ في Supabase مُحدَّث بقيم صحيحة — لأن `stores.push(xStore)` في `app.js` يُلحق/يُقدَّم على نتيجة Supabase لهذه الحقول. قبل أي `UPDATE` على `stores` في Supabase لمتجر قديم: ابحث أولاً هل يملك ملف `<slug>-data.js` مُحمَّلاً في `index.html` (`grep` عن اسم المتجر أو رقم `id` فيه) — إن وُجد، عدِّل الحقل في الملف نفسه (+رفع `?v=` في `index.html` و`sw.js`)، وSupabase بعدها اختياري/مرجعي فقط لهذا الحقل. مثال: إحداثيات متجر 17 (`afgan-data.js`) صُحِّحت في Supabase أولاً في 2026-07-12 ولم يظهر أي أثر على الموقع حتى عُدِّل الملف الثابت مباشرة.
- **بكسل فيسبوك (Meta Pixel) متناثر على 3 معرّفات مختلفة وعبر Business Managers منفصلة** — البكسل الحقيقي النشط فعلياً على الموقع (`meta_pixel_id` في `integration_settings`) قد لا يكون مرئياً أصلاً لحساب الإعلانات الذي تُنشأ عليه الحملات؛ تحقّق دائماً عبر Meta Business Suite (Data Sources → Datasets & pixels → تبويب Connected assets) قبل افتراض أن التتبّع يعمل. كما أن أي Business Manager جديد (أقل من ~عدة أسابيع) **يمنعه فيسبوك نفسه** من مشاركة أصوله (بكسل/جمهور) مع Business آخر عبر "Assign partner" — رسالة الخطأ: "Businesses new to Facebook Products can share this asset... after several weeks". انظر سطر 2026-07-12 أدناه (قرقوشة فرفوشة) للتفاصيل والحل المؤقت المطبَّق.

## 7. سجل الإصلاحات (Fix Log)

**أضف سطراً جديداً في أعلى الجدول (تحت رأس الجدول مباشرة) مع كل مهمة تنتهي منها. لا تحذف السجل القديم أبداً — هذا هو أساس منع تكرار نفس الخطأ ونسيان الحلول السابقة.**

| التاريخ | المشكلة | السبب الجذري | الإصلاح | الملفات المعدّلة | تم تحديث الإصدار في المواضع الثلاثة؟ |
|---|---|---|---|---|---|
| 2026-07-14 | المستخدم طلب إضافة مطعم «قلعة الشام» (Şam Kalesi / Qalaat Al-Sham) من كتالوج واتساب، معطياً رقم واتساب (+90 552 594 72 49) + عنوان (حي حريية، شارع محمود بيه، رقم 115، باهتشلي أوغلو) + ساعات عمل (12-23) | ميزة إضافة جديدة (لا يوجد خطأ سابق) — لكن الجلسة اصطدمت بعطل بيئي حاد: **تضارب جهاز واتساب ويب متعدد** (نافذة "WhatsApp is open in another window. Click 'Use here'") ظهر بشكل متكرر جداً (كل نقرة "Use Here" كانت تُستبدَل بنفس النافذة خلال ثوانٍ قليلة)، على الأرجح بسبب جلسة/جهاز آخر حقيقي يستخدم نفس حساب واتساب بالتوازي وينافس على "فتحة" الجلسة النشطة — أُعيد النقر على "Use Here" مرات عديدة إلى أن استقرت الجلسة لفترة كافية لإكمال الاستخراج. **درس تقني جديد يُضاف لدروس استخراج كتالوج واتساب السابقة:** كتالوج هذا المتجر (28 عنصراً بلا فرز فئات في القائمة) لا يُخفي الصور خلف blob بطيء التحميل كما في الجلسات السابقة (فستق حلب، فاتح الخير) — بل الصورة الكبيرة في لوحة "Details" (بعد النقر المباشر على كل صف) كانت متاحة فوراً بدقة عالية (1600×1000 تقريباً تلقائياً)، فكان التنزيل مباشراً (`fetch(blobUrl)→createObjectURL→anchor.click()`) بلا حاجة لانتظار إضافي، وبمعدل تنزيل واحد لكل نقرة حقيقية دون الاصطدام بسقف الـ10 تنزيلات المعروف من جلسات سابقة (على الأرجح لأن التنزيلات كانت متباعدة زمنياً بفعل تضارب الجهاز نفسه). **قائمة العناصر بلا فئات فرعية حقيقية في المصدر** (28 عنصراً متتالياً بلا عناوين أقسام)، فصُنِّفت يدوياً إلى 5 فئات منطقية (مقبلات، سندويشات، برغر، وجبات شاورما ودجاج، المناسف) حسب طبيعة كل صنف. **تكرار حقيقي واحد مؤكَّد ومدموج:** "سندويش مكسيكانو" ظهر مرتين بنفس الاسم والسعر بالضبط (200 ل.ت) بصورتين مختلفتين قليلاً لنفس الصنف (زاوية/إضاءة مختلفة) — اعتُمد النسخة الأولى فقط وأُهملت الثانية (27 منتجاً فريداً من أصل 28 عنصراً في القائمة). 🔴 **شذوذ سعر حقيقي غير محلول عمداً:** منتج "يالنجي" (ورق عنب) مسعَّر **20,000 ل.ت** في كتالوج التاجر نفسه — رقم غير منطقي مقارنة ببقية الأطباق المماثلة (كبة طرابلسية 80 ل.ت، حتى منسف لحم لأربعة أشخاص 2,400 ل.ت فقط) — على الأرجح خطأ إدخال من التاجر (ربما قصد 200 ل.ت)، لكن اتُّبعت سياسة عدم اختلاق/تصحيح بيانات التاجر من تلقاء نفسي؛ نُقِل السعر كما ورد حرفياً مع توثيق الشذوذ هنا للفت انتباه التاجر لاحقاً. **تحقّق عنوان/تقييم مزدوج عبر Google Maps:** البحث باسم "Şam Kalesi Restoran" (الاسم اللاتيني الظاهر فعلياً على واجهة المحل في أول صورة منتج "يالنجي" التي كانت في الحقيقة صورة واجهة المحل الحقيقية من الشارع مع طبق يالنجي في المقدمة، وليست صورة استوديو) أعاد نتيجة مطابقة تماماً: **"ŞAM KALESİ LOKANTASI"**، تقييم حقيقي 3.9 (515 تقييماً)، فئة "Arab restaurant"، عنوان **"Hürriyet, Mahmutbey Cd. No:117"** (وليس 115 كما أرسل المستخدم — فرق طفيف، اعتُمد رقم غوغل الأدق 117)، إحداثيات دقيقة (41.0012849, 28.8420208)، وساعات "Closes 11:30 PM" (قريبة جداً من نص المستخدم "حتى 23" فاعتُمد نص المستخدم الصريح "يومياً 12:00 ظهراً – 11:00 مساءً"). **رقم هاتف مختلف مكتشَف على واجهة المحل نفسها وعلى غوغل مابس (0552 291 51 75) عن رقم واتساب الذي أرسله المستخدم (+90 552 594 72 49)** — اعتُمد رقم واتساب المُرسَل صراحةً كحقل `phone`/`whatsapp` الوحيد للمتجر (هو قناة الطلب الفعلية)، ولم يُستخدم رقم غوغل/اللافتة إطلاقاً في أي حقل. صورة "يالنجي" (صورة واجهة محل حقيقية بلافتة "RESTAURANT ŞAM KALESİ / قلعة الشام" واضحة تماماً) اعتُمدت كغلاف المتجر أيضاً (بجانب كونها منتجاً) — نفس نمط إعادة استخدام صورة منتج حقيقية كغلاف حين لا يوجد بانر تسويقي مخصَّص (سابقة توثَّقت مراراً: توبيز، فاتح الخير). الشعار (أيقونة قلعة خضراء متجهة نظيفة على خلفية فاتحة، مأخوذة من صورة الملف الشخصي لواتساب بزنس 640×640) استُخدم كما هو. المتجر رقم **94** (تحقّق `max(id)`=93 مرتين: أول مرة عند التخطيط، وأخرى مباشرة قبل التنفيذ — صفر تصادم هذه المرة)، حزمة معرّفات المنتجات **1760001–1760027** (27 منتجاً بالضبط بعد استبعاد التكرار). صُغِّرت الصور بـ`sharp` (منتجات ≤800px JPEG q82 mozjpeg، شعار ≤400px PNG، غلاف ≤1280px JPEG q85 — من ~9.5MB خام لـ29 ملفاً إلى 2.4MB إجمالاً). `approval_status='pending'` عمداً (متجر طرف ثالث حقيقي غير مؤكَّد كشراكة دكانجي، نفس سياسة كل المتاجر المشابهة السابقة). وُصلت الأسلاك الأربعة الإلزامية: `app.js` (`stores.push`, `products.push`, `initialDeliverySettings` spread) + `store-slugs.js` (`94: "qalaat-alsham-restaurant"`) + `<script src="/qalaatalsham-data.js?v=1">` في `index.html` قبل `app.js` + إدراج مماثل في `sw.js` (`APP_SHELL`). الدفع عبر Supabase MCP `execute_sql` مباشرة (صف المتجر + 27 صف منتج، `jsonb_to_recordset`، معاملتان منفصلتان). تحقّق شامل عبر preview محلي بمنفذ مستقل تلقائي (`autoPort`، لتفادي تعارض «خادم جلسة أخرى يعمل على هذا المجلد» المذكور في تنبيه النظام): `/store/qalaat-alsham-restaurant` يعرض العنوان الصحيح (`دكانجي - مطعم قلعة الشام`) والوصف والعنوان الحقيقي وساعات العمل والهاتف، كل الفئات الخمس بعناصرها الـ27 وأسعارها الصحيحة من Supabase مطابقة تماماً، صور الغلاف/الشعار/كل المنتجات الـ27 (29 رابطاً إجمالاً) أعادت HTTP 200 فعلياً (تحقّق برمجي بـ`fetch HEAD` لكل رابط دفعة واحدة)، `/category/restaurants` لا يعرض المتجر إطلاقاً (سلوك `pending` صحيح مطابق للسوابق — الرابط المباشر يعمل رغم ذلك)، الصفحة الرئيسية تحمَّلت بلا أي أثر، صفر أخطاء كونسول على الصفحات الثلاث جميعاً | `qalaatalsham-data.js` (جديد)، `assets/photos/qalaatalsham/**` (جديد، 29 ملف: 27 منتج + شعار + غلاف)، `app.js`, `store-slugs.js`, `index.html`, `sw.js`, `CLAUDE.md` + صف `stores` id 94 وصفوف `products` 1760001-1760027 في Supabase | نعم — `npm run check:versions` ناجح (app.js v302، store-slugs.js v243، CACHE v221). **لم يُنفَّذ `git commit`/push بعد — بانتظار تعليمات المستخدم**
| 2026-07-14 | المستخدم طلب إضافة مطعم «فاتح الخير» (Fatih Al-Khair)، فرع فاتح، معطياً رابط كتالوج واتساب (`wa.me/c/905527373795`) + رابط Google Maps (حُلّ مسبقاً من جلسة التوجيه: Place ID `ChIJNef7wiG6yhQR45rZXbk_K1Q`، تقييم 3.7/640، إحداثيات دقيقة 41.0203663/28.9486926) | ميزة إضافة جديدة (لا يوجد خطأ سابق) — لكن ظهرت 3 مشاكل تقنية حقيقية أثناء التنفيذ: (1) **اكتشاف تصحيحي مهم في تسمية الفئات:** جلسة التوجيه (orchestrator) كانت قد نقلت اسمي فئتين كـ«سندويشات غربية»/«وجبات غربية» استناداً لتحويل نصي سابق، لكن الفحص المباشر لعناوين الفئات الحقيقية في كتالوج واتساب (بالقراءة البصرية من `screenshot`/`zoom`، وليس `read_page`/`get_page_text` النصي المعروف بخلط حرفي غ/ع سابقاً — موثّق كدرس من جلسة الوكالة 2026-07-13) أظهر أن الاسمين الحقيقيين هما **«سندويش عربي»** و**«وجبات عربي»** (وليس «غربية» رغم أن الأصناف كريسبي/زنجر/اسكلوب غربية الطابع — كذلك سمّاها التاجر نفسه). استُخدمت التسميات الصحيحة المؤكَّدة بصرياً في الفئتين. (2) **كتالوج واتساب بلا فرز حقيقي بالفئات في القائمة الرئيسية** (فئات المعاينة العلوية لكل قسم تعرض 3 عناصر فقط من كل قسم حقيقي في كتالوج التاجر — لا تطابق الـ59 صنفاً الكاملة)؛ الاستخراج الفعلي والموثوق كان فقط عبر قسم **«All items»** (يعيد سرد كل الأصناف الـ59 مرة واحدة لكل صنف تقريباً، مع تكرارات قليلة صريحة رُصدت وأُهملت — مثال: «وجبة شاورما عربي اصابع» ظهرت مكررة، وكذلك «صحن مسبحة» و«سارير كازوز»)، بترتيب مختلف تماماً عن ترتيب الفئات المنطقي المطلوب — استُخرجت الصور بالترتيب الفعلي للقائمة ثم أُعيد ربطها بالاسم/السعر الصحيحين من القائمة المُعدَّة مسبقاً وليس بالترتيب. (3) 🔴 **خطأ صور متسلسل حقيقي اكتُشف بالمراجعة اللاحقة (وليس افتراض النجاح):** القائمة (list view) في واتساب تعرض صوراً مصغَّرة 96×96 فقط غير كافية، فاستُخدم النقر في كل صنف لفتح لوحة «Details» التي تحمّل صورة عالية الدقة كـ`blob:` URL (حتى 1600×1067) — لكن تحميل هذا الـblob **يتأخر أحياناً عن تحديث نص الاسم/السعر المعروضين في نفس اللوحة**، فأول محاولة تنزيل فورية بعد النقر (بلا انتظار كافٍ) التقطت أحياناً صورة الصنف **السابق** المفتوح بينما كان النص المقروء صحيحاً للصنف الجديد؛ اكتُشف ذلك لاحقاً بفحص MD5 لكل الصور الـ59 بعد التصغير: مجموعة تطابق تام غير منطقية إطلاقاً بين «كولا 330 مل» (مشروب) و«فروج مشوي مع بطاطا» (طبق دجاج مشوي) — صورتان لا يُعقل أن يتشاركهما التاجر أبداً (خلافاً لِـ6 مجموعات تطابق أخرى منطقية تماماً: نسخ عادي/حار من نفس الطبق، أو مقاسات مختلفة لنفس الساندويتش، التي أُبقيت كما هي). أُعيد فتح تفصيل «فروج مشوي مع بطاطا» تحديداً مع انتظار 2 ثانية إضافية والتحقق البصري من الصورة المعروضة **قبل** التنزيل (لقطة شاشة تؤكد طبق دجاج مشوي حقيقي مع بطاطا وشيبس) ثم تنزيلها وإعادة معالجتها بـ`sharp` لتحل محل الملف الخاطئ — هذا الدرس (انتظار تحميل صورة الـblob فعلياً قبل التنزيل، لا الاكتفاء بتحقق النص) يُضاف لدروس استخراج كتالوج واتساب السابقة (فستق حلب 2026-07-12، الوكالة 2026-07-13). طريقة التنزيل النهائية: تنزيل حقيقي عبر `fetch(blobUrl)`→`URL.createObjectURL`→`<a download>`→`click()` صناعي (وليس عبر `element.click()` البرمجي على صف القائمة نفسه، الذي فشل بصمت مرتين — التنقل الفعلي بين الأصناف تطلّب نقرات `computer` حقيقية بدل الأحداث المُصطنعة)، بمعدل عنصر واحد لكل استدعاء (لا سقف تنزيلات جماعي هذه المرة لأنها تنزيلات منفردة متتالية عبر نقرات حقيقية، لا دفعة واحدة). قائمة «All items» تستخدم عرضاً افتراضياً (virtualized list) — موضع التمرير (`scrollTop`) لحاوية القائمة يُصفَّر أحياناً بعد الرجوع من لوحة التفاصيل (لا يُحفَظ دائماً كما افتُرض أول الأمر)، فاعتُمد ضبط `scrollTop` مباشرة عبر JS إلى نفس القيمة قبل كل نقرة (بدل الاعتماد على `scroll` بالعجلة تراكمياً) لضمان دقة الإحداثيات في كل مرة. الشعار (`FATIH ALHAYR RESTAURANT`، نص ذهبي على دائرة خضراء مع شريط علم الثورة السورية الثلاثي الألوان وثلاث نجوم حمراء) نُزِّل من الصورة الشخصية الكاملة الدقة لملف تعريف واتساب بزنس (640×640، غير blob هذه المرة — رابط CDN مباشر). لا يوجد بانر غلاف حقيقي في الكتالوج (فقط رسم تسويقي بخلفية خضراء ونص الاسم)، فاستُخدمت بدلاً منه صورة منتج حقيقية عالية الجودة (صحن شاورما ملفوفة مع بطاطا وكول سلو ومخلل، تصوير استوديو احترافي بجودة ممتازة) كغلاف — نفس نمط سابقتي Two2Bees وفلوكا. المتجر رقم **93** (تحقّق `max(id)` مرتين: أول مرة عند التخطيط وأخرى مباشرة قبل التنفيذ، طبقاً للدرس المتكرر في السجل — لم يوجد أي تصادم هذه المرة)، حزمة معرّفات المنتجات **1750001–1750059** (59 صنفاً بالضبط، تحقّق صفر تصادم في النطاق قبل الإدراج). 6 مجموعات تكرار صور منطقية (نسخة حارة/عادية أو حجم مختلف لنفس الطبق) أُبقيت كما هي عمداً — لا تخالف قاعدة صورة فريدة لكل مسار ملف (كل مسار له ملف مستقل فعلاً، فقط المحتوى البصري نفسه من التاجر). صُغِّرت الصور بـ`sharp` (منتجات ≤800px JPEG q82، شعار ≤400px PNG، غلاف ≤1280px JPEG q85). `approval_status='pending'` عمداً (متجر طرف ثالث حقيقي غير مؤكَّد كشراكة دكانجي، نفس سياسة فستق حلب/الوكالة). وُصلت الأسلاك الأربعة الإلزامية: `app.js` (`stores.push`, `products.push`, `initialDeliverySettings` spread) + `store-slugs.js` (`93: "fatih-alkhair-restaurant"`) + `<script src="/fatihalkhair-data.js?v=1">` في `index.html` قبل `app.js` + إدراج مماثل في `sw.js` (`APP_SHELL`). الدفع عبر Supabase MCP `execute_sql` مباشرة (صف المتجر + 59 صف منتج، `jsonb_to_recordset`، معاملتان منفصلتان). تحقّق شامل عبر preview محلي: `/store/fatih-alkhair-restaurant` يعرض العنوان الصحيح (`دكانجي - مطعم فاتح الخير`) والوصف والعنوان الحقيقي وساعات العمل (`يومياً 10:00 ص – 2:00 ص`) والهاتف بشكل صحيح، كل الفئات الثمانية بعناصرها الـ59 وأسعارها الصحيحة من Supabase مطابقة تماماً، صور الغلاف/الشعار/كل المنتجات الـ61 صورة (59+شعار+غلاف) أعادت HTTP 200 فعلياً (تحقّق برمجي بـ`fetch HEAD` لكل رابط)، `/category/restaurants` يعرض 38 متجراً بلا أي أثر لمطعم فاتح الخير (سلوك `pending` صحيح مطابق لسابقة الوكالة — الرابط المباشر يعمل رغم ذلك)، صفر أخطاء كونسول على الصفحتين والرئيسية | `fatihalkhair-data.js` (جديد)، `assets/photos/fatihalkhair/**` (جديد، 61 ملف: 59 منتج + شعار + غلاف)، `app.js`, `store-slugs.js`, `index.html`, `sw.js`, `CLAUDE.md` + صف `stores` id 93 وصفوف `products` 1750001-1750059 في Supabase | نعم — `npm run check:versions` ناجح (app.js v301، store-slugs.js v242، CACHE v220). **لم يُنفَّذ `git commit`/push بعد — بانتظار تعليمات المستخدم**
| 2026-07-14 | المستخدم طلب إضافة مطعم «تشاركو تشيكن» (Charco Chicken) فرع فاتح، معطياً رابط Linktree (`linktr.ee/Charcochicken`) + رابط Google Maps + هاتف (+90 501 262 22 23) + ساعات عمل (12:30 ظهراً – 11:00 مساءً) | ميزة إضافة جديدة (لا يوجد خطأ سابق) — سلسلة عالمية بفرعين في إسطنبول (فاتح وبيليك دوزو)؛ رابط Linktree يوزّع الطلب حسب الفرع عبر أرقام واتساب مختلفة، فكان لا بد من تأكيد الفرع الصحيح أولاً | حُلّ رابط Google Maps المختصر فعلياً (`maps.app.goo.gl/kxDJuvDoNfcUXccy5`) فأعطى "Charco Chicken" في فاتح بإحداثيات دقيقة (41.0199967, 28.9411767) وتقييم Google حقيقي 4.6 (1703 تقييماً) ورقم هاتف مطابق حرفياً للرقم المُرسَل (+90 501 262 22 23) — يؤكد أن هذا فرع فاتح تحديداً وليس بيليك دوزو. الموقع الفرعي `tr.charcochicken.com.tr` (مكتشَف من رابط Linktree) يحوي قائمة كاملة كصورة واحدة مركّبة (`menu.jpg`، 953×1800px) بدل قائمة HTML — استُخرجت 15 صورة منتج فردية منها بالقص الدقيق عبر `sharp` (تحقّق تكراري بعرض كل قصّة قبل الاعتماد لضمان عدم بتر السعر/الاسم)، بدل استخدام صورة واحدة مكررة لكل المنتجات. القائمة صغيرة ومباشرة (5 أصناف دجاج كامل مشوي بالفحم 950 ل.ت لكل صنف، صنفان جانبيان أرز/خبز، 3 صوصات خاصة، 4 أطباق حمص/سلطة + علبة مكس) فقُسِّمت 4 فئات منطقية. الشعار (`charcochicken.com.tr/wp-content/uploads/2024/12/logog1.png`) نص أبيض شفاف يختفي على خلفية فاتحة — اكتُشف ذلك بتركيبه فعلياً على خلفية داكنة عبر `sharp` قبل الاعتماد، فأُنتج شعار نهائي على خلفية سوداء مربعة 400×400. المتجر رقم **92** (بعد التحقق من `max(id)`=91 مباشرة قبل التنفيذ)، حزمة معرّفات المنتجات **1740001–1740015** (تحقّق صفر تصادم في النطاق قبل الإدراج). `approval_status='approved'` مباشرة (صور محلية منشورة). لقطات الشاشة (`computer screenshot`) كانت معطّلة تماماً طوال الجلسة (سبق توثيقه كعطل بيئي متكرر) — الاعتماد تم عبر تنزيل الصور بـ`curl` وقراءتها مباشرة بأداة `Read`، وتحقّق نص/شبكة/كونسول كامل عبر preview محلي بدل اللقطات المرئية. وُصلت الأسلاك الأربعة الإلزامية: `app.js` (`stores.push`, `products.push`, `initialDeliverySettings` spread) + `store-slugs.js` (`92: "charco-chicken-fatih"`) + `<script src="/charcochicken-data.js?v=1">` في `index.html` + إدراج مماثل في `sw.js` (`APP_SHELL`). الدفع عبر Supabase MCP `execute_sql` مباشرة (صف المتجر + 15 صف منتج، `jsonb_to_recordset`). تحقّق شامل عبر preview محلي: `/store/charco-chicken-fatih` يعرض العنوان والوصف والعنوان الحقيقي وساعات العمل والهاتف بشكل صحيح، كل الفئات الأربع بمنتجاتها الـ15 وأسعارها الصحيحة، صور الغلاف/الشعار/المنتجات كلها HTTP 200، `/category/restaurants` يعرض المتجر ضمن قائمة الـ41 مطعماً بمعدل «٢٠ ل.ت/كم»، صفر أخطاء كونسول على الصفحتين | `charcochicken-data.js` (جديد)، `assets/photos/charcochicken/**` (جديد، 17 ملف: 15 منتج + شعار + غلاف)، `app.js`, `store-slugs.js`, `index.html`, `sw.js`, `CLAUDE.md` + صف `stores` id 92 وصفوف `products` 1740001-1740015 في Supabase | نعم — `npm run check:versions` ناجح (app.js v300، store-slugs.js v241، CACHE v219). **لم يُنفَّذ `git commit`/push بعد — بانتظار تعليمات المستخدم** |
| 2026-07-14 | المستخدم طلب «إكمال عملية الأندكسنغ للمتاجر — إدراج رابط كل متجر حتى يراه غوغل كصفحة» | ليس خطأ — مهمة بيانات/تشغيل. تحقّق فعلي (وليس افتراض) أن `sitemap-stores.xml` الحي يعكس بالفعل كل المتاجر الحية: 77 رابطاً مطابقاً تماماً لكل صف `stores` بشرط `open=true` و(`approval_status` فارغ أو `approved`) — نفس فلتر `api/sitemap.js`. الاستثناءان الوحيدان (id 18 «أيام شامية» بسبب `open=false`، وid 20 «يمن شيف» المرفوض) استثناءان مقصودان صحيحان وليسا نقصاً. أي متجر جديد يُضاف لـ`store-slugs.js` مع `approval_status='approved'` و`open=true` يظهر في الـsitemap تلقائياً بلا أي عمل إضافي — القناة السلبية (sitemap→robots.txt→زحف غوغل الدوري) تعمل فعلاً لكل الـ77. **العائق الفعلي للدفع الاستباقي عبر Google Indexing API تحديداً:** `api/notify-google.js` (منذ تأمين نقاط الـAPI في commit `0de73326`) يتطلب جلسة إدارية/تاجر صالحة (`x-admin-token` موقَّع بمفتاح `ADMIN_SESSION_SECRET`/`ADMIN_PASSWORD` الإنتاجي على Vercel) — وهذا المفتاح الإنتاجي **غير متاح** لا في `.env` المحلي (يحوي فقط `dukkanci-dev` الخاص بالتطوير المحلي، تحقّقت بمحاولة استخدامه على الموقع الحي فأعاد 401 فعلياً) ولا عبر أي أداة Vercel متصلة (لا توجد أداة لقراءة متغيرات البيئة). سياسة الأمان تمنعني أيضاً من إدخال كلمة مرور الإدارة بنفسي حتى لو أرسلها المستخدم مباشرة، فالحل الآمن الوحيد هو أن يُشغِّل المستخدم نفسه سكربتاً جاهزاً بمفتاحه الإنتاجي كمتغيّر بيئة محلي دون كتابته في المحادثة إطلاقاً | أُنشئ `scripts/submit-store-urls.mjs` (نسخة مركَّزة على المتاجر فقط من `submit-all-urls.mjs` الموجود مسبقاً، الذي كان يفتقد أصلاً ترويسة المصادقة اللازمة بعد تأمين الـendpoint فكان سيفشل بـ401 على كل رابط): يجلب فقط المتاجر الحية فعلياً (نفس فلتر `open=true` + `approval_status` من Supabase مباشرة) ويُرسلها لـ `/api/notify-google` مع ترويسة `x-admin-key` من متغيّر بيئة `ADMIN_KEY` (لا قيمة افتراضية مضمَّنة). **لم يُنفَّذ الإرسال الفعلي بعد** — بانتظار أن يُشغِّل المستخدم: `ADMIN_KEY=<كلمة مرور لوحة الإدارة الإنتاجية على dukkanci.com.tr> node scripts/submit-store-urls.mjs` من جهازه (77 رابطاً، ضمن حصة Google اليومية ~200 بأمان) | `scripts/submit-store-urls.mjs` (جديد)، `CLAUDE.md` | لا ينطبق (لم يُعدَّل `app.js`/`styles.css`/`index.html`/`sw.js`) |
| 2026-07-13 | المستخدم طلب تحسين غلاف وشعار متجر «الوكالة» (كانا صورتي واتساب بزنس الأصليتين للتاجر — شعار دائري أخضر بسيط + بانر بخلفية جبل) | ليس خطأ — طلب تحسين تصميم مباشر بعد نشر المتجر (نفس اليوم). أول استخدام لتوليد صور (nano_banana) لمتجر جاء أصلاً من كتالوج واتساب بزنس (خلافاً لسوابق إعادة تصميم أغلفة متاجر «مدموجة يدوياً» قديمة كزمزم) | وُلِّدت 3 شعارات و4 مقترحات غلاف (`generate_image`, nano_banana_2 فعلياً تحت الاسم المطلوب nano_banana_pro) بهوية موحّدة (أخضر غامق + ذهبي، أيقونة سنبلة+سلة تسوق، خط عربي واضح لكلمة «الوكالة»). **اكتُشف عيب حقيقي في أول مقترح غلاف قبل اعتماده:** محاكاة قصّ `object-fit:cover` الفعلي عبر `sharp` لكلا حاويتي العرض الحقيقيتين في `styles.css` (`.store-cover` ارتفاع 310px بعرض حاوية 1240px = نسبة هيرو 4:1، و`.store-card__image` ارتفاع 205px = نسبة بطاقة تقريبية 1.46:1) أظهرت أن نسبة الهيرو الأوسع (4:1) تقصّ من الأعلى/الأسفل بما يكفي لبتر الشعار الفرعي «بقالة مصرية أصيلة» بالكامل تقريباً — لأن العناصر لم تكن مجمَّعة بإحكام كافٍ عمودياً في التصميم الأول. أُعيد التوليد بقيد صريح إضافي في الطلب (أعلى/أسفل الصورة 22% خلفية بحتة بلا نص، كل العناصر النصية والشعار داخل الشريط الأوسط 56% فقط)، ثم أُعيد فحص القصّين ببرنامج `sharp` مرة أخرى فعلاً (وليس افتراض النجاح) قبل الاعتماد — كلا القصّين أظهرا الشعار والعنوان والشعار الفرعي كاملين بلا بتر. الشعار المُختار (نص أبيض على حلقة ذهبية، تباين أعلى وأوضح عند العرض الصغير الفعلي ~66px في الواجهة) بدل بديلين بنص ذهبي على أخضر (تباين أقل). صُغِّرت الصور النهائية بـ`sharp` (غلاف ≤1280px JPEG q85 mozjpeg، شعار ≤400px PNG). **حادثة تزامن أثناء الحفظ:** جلسة أخرى كانت تعمل بالتوازي على `index.html`/`sw.js` (إضافة متجر «تو بيز» ورفع `app.js` إلى v299 و`CACHE` إلى v218) — عُزلت تعديلاتي (رفع `wekala-data.js?v=1→2` فقط لتفعيل مسار الشعار الجديد `logo.png`، و`CACHE v216→v217` كنسخة مستقلة) بإزالة سطر الجلسة الأخرى وخفض رقم `app.js` مؤقتاً لحالة `HEAD` الفعلية قبل الالتزام، ثم أُعيدت تعديلات الجلسة الأخرى كما كانت في نسخة العمل غير الملتزمة بعد الالتزام مباشرة — نفس أسلوب `git apply --cached` الموثَّق سابقاً لكن بتحرير/استعادة يدوي بما أن التغييرات كانت أسطراً متجاورة يستحيل فصلها بترقيع تلقائي. **تحديث Supabase مباشر أيضاً:** `stores.logo_image` لصف المتجر (id 90) حُدِّث فوراً إلى `/assets/photos/wekala/logo.png` (المسار القديم `logo.jpg` حُذف من المستودع) — اكتُشف أثناء العمل أن المتجر أصبح `approval_status='approved'` (لم يعد `pending` كما تُرك في الجلسة السابقة نفس اليوم — على الأرجح اعتمده المستخدم أو الإدارة يدوياً)، مما يعني أنه مرئي على الموقع الحي فعلاً؛ نظراً لأن تعديل Supabase يسبق نشر ملف الصورة الفعلي على Vercel، رُفع الالتزام لـ`main` فوراً بعد التحقق المحلي (بدل الانتظار) لتفادي نافذة صورة مكسورة حقيقية على الموقع الحي (نفس تحذير «انتظر نشر الصور قبل الاعتماد» من [[adding-a-restaurant]] لكن بالاتجاه المعاكس هنا: الاعتماد سبق النشر بفعل جهة خارجية، فالحل هو تسريع النشر لا تأخير الاعتماد). تحقّق نهائي عبر preview محلي: طلبا الشبكة لـ`cover.jpg`/`logo.png` أعادا HTTP 200، و`stores.find(s=>s.id===90).logoImage` يعكس المسار الجديد بشكل صحيح | `wekala-data.js`, `index.html`, `sw.js`, `assets/photos/wekala/cover.jpg` (استبدال في مكانه), `assets/photos/wekala/logo.png` (جديد، حذف `logo.jpg` القديم), `CLAUDE.md` + عمود `stores.logo_image` للمتجر id 90 في Supabase | نعم — `npm run check:versions` ناجح؛ **مدفوع مباشرة لـ`main`** (commit منفصل عن إضافة المتجر الأصلية) بعد تحقّق محلي كامل، بناءً على طلب المستخدم الصريح بالنشر في الرسائل السابقة لنفس المتجر في نفس الجلسة |
| 2026-07-13 | المستخدم طلب إضافة متجر «تو بيز» (Two2Bees، عسل طبيعي ومنتجات عشبية) ضمن قسم «مواد غذائية متخصصة»، معطياً رابط Shopier (`shopier.com/two2bees`) + هاتف (`+90 501 120 01 00`) + ملاحظة أن المتجر في **مرسين** ويوصّل عبر البريد المحلي | **أول متجر يكسر افتراض «توصيل محلي حي بالكيلومتر» الذي بُني عليه دكانجي بالكامل** — سقف `maxRoundTripKm` في الكود لا يتجاوز 200كم، بينما مرسين تبعد عن إسطنبول ~700كم؛ أي عميل حقيقي كان سيرى «العنوان خارج نطاق التوصيل» دائماً لو عومل المتجر كبقية المتاجر. طُرح هذا صراحة على المستخدم عبر `AskUserQuestion` قبل البدء (بدل افتراض حل تقني) فاختار: «أضف المتجر كدليل/عرض فقط بلا زر شراء فوري» | اكتُشف أن آلية `priceOnRequest` الموجودة أصلاً (مستخدَمة سابقاً لمتجر كنافة البراء 68) تحل المشكلة **بلا أي تعديل كود جديد**: تعيين `priceOnRequest:true` على كل منتج يُسقط زر «أضف للسلة» تلقائياً (`addToCart` في `app.js` يتوقف فوراً) ويستبدله بزر «اطلب عبر واتساب» حقيقي (`waOrderLink`) يفتح محادثة مباشرة مع رقم المتجر — فلا تُحسب رسوم توصيل ولا يُفتح سلة إطلاقاً لهذا المتجر، فتُتجنَّب مشكلة الـ200كم من جذورها بدل الالتفاف عليها. **الكتالوج (108 منتجاً معلنة، 102 فريدة فعلياً):** موقع Shopier SPA لا يُحمِّل المنتجات في الـHTML مباشرة، بل عبر `POST /s/api/v1/search_product/two2bees` (تصفّح شبكي حقيقي فقط عبر نقر تبويبات الفئة الفعلية — `fetch()` مباشر من نفس الصفحة أعاد 404/500/فارغاً باستمرار، على الأرجح تحقق من نمط طلب داخلي لا يمكن تقليده ببساطة). قراءة الاستجابة الكاملة عبر أداة فحص الشبكة اصطدمت بسقف اقتطاع النص عند الفئات الكبيرة، فاستُخدم استخراج DOM بعد كل نقرة تبويب كبديل موثوق (نفس درس فستق حلب حول أن `element.click()` البرمجي قد لا يكفي لبعض الأطر، هنا كان يكفي لكن قراءة الشبكة لا). فئات المصدر التسع (بلا تبويبي «الكل»/«عروض» المتقاطعين) أعطت 109 صفاً خاماً تقلَّصت إلى 102 معرّف فريد بعد استبعاد 7 منتجات ظهرت في أكثر من تبويب فئة واحد (نفس المنتج، نفس id). هذه المهمة الميكانيكية (نقر 9 تبويبات + قراءة DOM + تنزيل 102 صورة منتج + الشعار + الغلاف) فُوِّضت لوكيل خلفي مستقل بتوجيهات تقنية دقيقة (نقطة الـAPI، خريطة أسماء الفئات→cat_id، نمط رابط CDN) بعد أن أثبتُّ يدوياً أنها تعمل — أعاد الوكيل 102/102 صورة بلا فشل واحد. **لا يوجد بانر عرضي حقيقي على المصدر** (فقط الشعار مكرَّراً)، فاستُخدمت بدلاً من الغلاف صورة منتج حقيقية عالية الجودة (عسل السدر 850غ، خلفية بيضاء نظيفة تُظهر العلامة التجارية بوضوح) كغلاف المتجر — نفس نمط سابقة فيلوكا حين لا يوجد بانر مخصَّص. **إحداثيات دقيقة عبر Google Maps الحقيقي** (لا تخمين): TikTok الرسمي للمتجر تضمَّن رابط Google Maps مباشراً لمحلّه الفعلي في مرسين (`maps.app.goo.gl/cDm9gFifhCE2ycve8`) يطابق الاسم/الهاتف حرفياً — حُلَّ الرابط المختصر فأعطى عنواناً دقيقاً (ينی شهير، مرسين) وإحداثيات (36.7798888, 34.5800086) وتقييم Google حقيقي 5.0. **ساعات العمل غير مكتملة على غوغل** (فقط «يفتح الثلاثاء 9:00» بلا جدول أسبوعي كامل) فاستُخدم نص صادق «يُرجى التواصل عبر واتساب لتأكيد ساعات العمل» بدل اختلاق جدول كامل. المتجر رقم **91** (تحقّق مزدوج من `max(id)` قبل الكتابة مباشرة — رُصد تصادمان حيّان أثناء العمل: معرّف 90 أُخذ بالفعل من جلسة متزامنة أضافت متجر «الوكالة» بين أول فحص وفحص لاحق، ونطاق معرّفات المنتجات الأول المخطَّط له (1720001+) تبيَّن أنه صار مستخدَماً جزئياً [52 صفاً] لنفس متجر الوكالة، فأُعيد الاختيار لنطاق 1730001-1730102 بعد تحقّق صفر تصادم مباشر قبل التنفيذ). صُغِّرت الصور بـ`sharp` (منتجات ≤800px JPEG q82، شعار ≤400px، غلاف ≤1280px، من ~5-6MB خام إلى 3.0MB إجمالاً لـ104 ملف). أُنشئ `two2bees-data.js` (نمط `beitbeyrut-data.js`، مع كل منتج يحمل `priceOnRequest:true` صراحة في دالة البناء وليس فقط على مستوى المتجر). **`approval_status='approved'` مباشرة** (الصور محلية بالفعل، لا حاجة انتظار نشر). وُصلت الأسلاك الأربعة الإلزامية: `app.js` (`stores.push`, `products.push`, `initialDeliverySettings` spread) + `store-slugs.js` (`91: "two2bees-honey-mersin"`) + `<script src="/two2bees-data.js?v=1">` في `index.html` + إدراج مماثل في `sw.js` (`APP_SHELL`). الدفع عبر Supabase MCP `execute_sql` (`jsonb_to_recordset`، معاملتان: صف المتجر و102 صف منتج، مع الاحتفاظ بالسعر الحقيقي في عمود `price` رغم إخفائه في الواجهة بسبب `price_on_request` — خلافاً لسابقة كنافة البراء التي خزَّنت 0 لأنها لم تملك سعراً حقيقياً أصلاً؛ هنا السعر حقيقي فحُفظ كبيانات صحيحة للرجوع إليها مستقبلاً بلا أي أثر وظيفي لأن الواجهة تتجاهله بالكامل عند `price_on_request=true`). **حادثتا تزامن أثناء التنفيذ:** (1) `sw.js`/`index.html` تلقّتا تعديلاً خارجياً منتصف العمل (رفع `wekala-data.js?v=1→2` و`CACHE v217→v218`) اكتُشف عبر تنبيه نظام تلقائي، فأُعيد فحص الملفين مباشرة بعده وتأكَّد أن إضافتي (`two2bees-data.js`, `app.js?v=299`) بقيت سليمة بجانب تعديل الجلسة الأخرى بلا تعارض حقيقي؛ (2) نفس نمط تصادم معرّفات موثَّق أعلاه. تحقّق شامل عبر preview محلي: `/store/two2bees-honey-mersin` يعرض العنوان والوصف الصحيحين، الشعار والغلاف (HTTP 200)، كل الفئات التسع بعناصرها الصحيحة (102/102 من Supabase)، **كل بطاقة منتج تُظهر «السعر عند الطلب» + زر واتساب حقيقي يفتح `wa.me/905011200100` برسالة تتضمن اسم المنتج فعلياً (تحقّق مباشر عبر DOM: `addToCartButtonsCount:0`, `waButtonsCount:102`)**، `/category/specialty-food` يعرض المتجر ضمن القائمة، صفر أخطاء كونسول على كلتا الصفحتين. رُفع الإصدار: `app.js` v298→299، `CACHE` v218 (كان مرفوعاً بالفعل من الجلسة المتزامنة، أُبقي كما هو لأنه يغطي تعديلي أيضاً)، و`npm run check:versions` ناجح | `two2bees-data.js` (جديد)، `assets/photos/two2bees/**` (جديد، 104 ملف: 102 منتج + شعار + غلاف)، `app.js`, `store-slugs.js`, `index.html`, `sw.js`, `CLAUDE.md` + صف `stores` id 91 وصفوف `products` 1730001-1730102 في Supabase | نعم — `npm run check:versions` ناجح. **لم يُنفَّذ `git commit`/push بعد — بانتظار تعليمات المستخدم** |
| 2026-07-13 | المستخدم طلب إضافة متجر «الوكالة للبقالة المصرية» ضمن قسم سوبر ماركت، معطياً رابط كتالوج واتساب (`wa.me/c/225829783093386` — تبيّن أنه رقم خاطئ، رقم واتساب الصحيح بعد سؤال المستخدم: `+90 536 571 79 83`) + موقع «شيرين ايفلر، إسطنبول» (وعد المستخدم بتحديده بدقة لاحقاً) | ميزة إضافة جديدة (لا يوجد خطأ سابق) — أول جلسة تُطوِّر منهجية **استخراج بنيوي** لكتالوج واتساب بدل الاعتماد على `row.textContent` المسطَّح (الأسلوب المستخدم سابقاً مع فستق حلب في 2026-07-12): الاسم/الوصف/السعر عناصر DOM منفصلة قابلة للتمييز بـ`font-size`/`color` (الاسم 16px أسود، الوصف والسعر 14px رمادي) — استخراجها منفصلة بدل النص المدمج يتفادى التباسات مثل "جبنة براميلي بالفلفلنصف كيلو" (لا فاصل نصي بين الاسم والوصف حين لا يوجد سطر جديد في المصدر) | **الرابط الأول كان معطوباً فعلاً** — `wa.me/c/225829783093386` أعاد صفحة 404 حقيقية من واتساب (رقم 15 خانة، أطول من رقم تركي عادي ~12 خانة)؛ التحقّق تم بفتح الرابط فعلياً في Chrome حقيقي مسجَّل الدخول (وليس افتراض فوري)، ثم سؤال المستخدم عبر `AskUserQuestion` أعطى الرقم الصحيح. **درسان جديدان في استخراج كتالوج واتساب (يُضافان لدرسَي فستق حلب):** (1) الكتالوج مقسَّم بصرياً لمجموعات فئات (٦-٧ عنصر لكل فئة، أعلى الصفحة) **ثم** قسم «All items» أسفله يُعيد سرد **كل** المنتجات مجدداً بلا فئات — يجب استخراج الفئات الحقيقية من الأقسام العلوية المسمّاة (وليس افتراض أسماء فئات) والتوقف عن العدّ عند وصول «All items» لأنه لا يضيف عناصر جديدة (تحقّق: `added:0` لعدة تمريرات سكرول متتالية) لكنه قد يكشف عناصر **مفقودة فعلياً** من الاستخراج الأول (هنا 3 عناصر: أسماك رنجة + بندا قهوة الشام مرتين) بصيغة نص خام بديلة تحتاج تنظيفاً يدوياً. (2) **`window.URL.createObjectURL` + نقر `<a download>` صناعي يصطدم بسقف متصفّح صامت: بالضبط 10 تنزيلات تلقائية تنجح ثم تتوقف الباقي بصمت (لا خطأ JS، fetch ناجح، لكن لا ملف على القرص)** — هذا قيد أمان Chrome لمنع "تنزيلات تلقائية متعددة" بلا لفتة مستخدم حقيقية؛ التفافه عبر إرسال البيانات لخادم Node محلي (`fetch('http://127.0.0.1:PORT')`) رُفض من مصنّف الصلاحيات (إضعاف أمني: تجاوز حماية Private Network Access). **الحل المتوافق مع القواعد:** الدُفعات بحجم ≤10 (نفس السقف)، معالجة/نقل كل دفعة (`mv` من Downloads إلى scratchpad) قبل طلب الدفعة التالية — تبيّن تجريبياً أن السقف **يتعافى تلقائياً خلال ثوانٍ** (لا حاجة لإعادة تحميل الصفحة)، فالإبطاء الوحيد هو عدد جولات الأداة لا إعادة تصميم كامل. **درس تصادم-أسماء صامت:** 3 من أصل 55 صورة تنزيل كانت **تكراراً حرفياً بنفس الاسم/السعر/الصورة** (منتج ظهر مرتين في فئتين مختلفتين من كتالوج التاجر — «ترمس مر»، «رنجة بطروخ»، «كركديه») اكتُشف عبر مقارنة MD5 لكل الصور الـ55 بعد التنزيل (3 مجموعات تطابق تام) — أُزيلت الثلاثة المكرَّرة (52 منتجاً فريداً فعلياً)؛ 3 مجموعات أخرى تشارك صورة واحدة بين منتجين مختلفين فعلاً (نفس صورة أرز لعبوتَي 2.5/5 كيلو، نفس صورة فطيرة كبيرة/صغيرة، نفس صورة لثلاثة مخللات) — أُبقيت كمنتجات منفصلة لأن كل واحد له مسار ملف مستقل بالفعل (لا يخالف قاعدة "صورة فريدة لكل مسار" من [[product-publishing-rules]]، فقط المحتوى البصري مكرَّر من التاجر نفسه). **درس محاذاة بيانات دقيق:** الدمج بين الاستخراج البنيوي (٤٩ عنصراً تلقائياً) والتصحيح اليدوي لـ5 عناصر فشل استخراجها آلياً (تنسيق نص غير عادي) يتطلب مطابقة **موضعية دقيقة** لا افتراض تتابع بسيط — أول محاولة دمج بترتيب مواضع خاطئ سبّبت انزياحاً تسلسلياً لكل الأسعار بعد أول عنصرين مصحَّحين يدوياً؛ أُصلح بحذف العنصرين المطابقين من قائمة الاستخراج الآلي صراحة (`splice` بموضعهما الحقيقي) قبل المحاذاة التتابعية، ثم تحقّق آلي أن كل سعر مطابق 100% بين المصدرين قبل الاعتماد. المتجر رقم **90** (بعد التحقق من `max(id)`=89 مرتين، أول مرة وأخرى قبل التنفيذ مباشرة)، حزمة معرّفات المنتجات **1720001–1720052**. 16 فئة حقيقية مطابقة لتقسيم كتالوج التاجر نفسه (أجبان، شاي، قهوة، ألبان، حلويات مصرية، تسالي ومكسرات، أسماك مملحة، أرز وبقوليات، تمور ومجففات، مجمدات، مخبوزات، عطارة وبهارات، زيوت ودهون، مشروبات طبيعية، معلبات، مخلل وزيتون). شعار وغلاف المتجر (لا بلوب — رابطا `cdn.whatsapp.net` حقيقيان مع توكن استعلام، حُمِّلا بنفس أسلوب fetch+anchor بلا طباعة الرابط لتفادي حجب الأداة لبيانات كوكي/استعلام). صُغِّرت الصور بـ`sharp` (منتجات ≤800px JPEG q82، شعار ≤400px، غلاف ≤1280px). أُنشئ `wekala-data.js` (نمط `beyazit-data.js`). **⚠ الإحداثيات تقريبية عمداً** (مركز حي شيرين ايفلر العام 40.9971°/28.8662°) — المستخدم صرَّح أنه سيحدّد الموقع الدقيق لاحقاً؛ بحث Google Places بالاسم العربي/الإنجليزي لم يُرجع أي نتيجة حقيقية (كما متوقَّع لعمل غير مسجَّل على غوغل). **`approval_status='pending'` عمداً** (طرف ثالث حقيقي غير مؤكَّد كشراكة دكانجي، نفس سياسة فستق حلب). وُصلت الأسلاك الأربعة الإلزامية: `app.js` (`stores.push`, `products.push`, `initialDeliverySettings` spread) + `store-slugs.js` (`90: "al-wekala-egyptian-market"`) + `<script src="/wekala-data.js?v=1">` في `index.html` قبل `app.js` + إدراج مماثل في `sw.js` (`APP_SHELL`). الدفع عبر Supabase MCP `execute_sql` (`jsonb_to_recordset`، معاملة واحدة للمتجر و52 منتجاً). تحقّق شامل عبر preview محلي: `/store/al-wekala-egyptian-market` يعرض العنوان الصحيح (`دكانجي - الوكالة`)، الشعار والغلاف (HTTP 200)، كل الفئات الـ16 بعناصرها الصحيحة (52/52 من Supabase، البيانات المحمَّلة محلياً فارغة عمداً)، صفر أخطاء كونسول على صفحة المتجر والرئيسية؛ **تحقُّق إضافي مهم:** `/category/supermarket` أظهر المتجر للحظة عابرة في أول لقطة نص (قبل اكتمال تحميل الكتالوج الكامل من Supabase، حين يكون الكائن المُجمَّع محلياً بلا حقل `approvalStatus` فيُعامَل افتراضياً كمعتمد) ثم اختفى بشكل صحيح بعد اكتمال التحميل (تحقّق مباشر عبر استدعاء `isStoreApproved` في كونسول المتصفح) — سلوك متوقَّع وليس عطلاً، موثَّق هنا لتفادي إنذار كاذب مستقبلاً. رُفع الإصدار: `app.js` v297→298، `store-slugs.js` v239→240، `CACHE` v215→216 في `sw.js`، و`npm run check:versions` ناجح | `wekala-data.js` (جديد)، `assets/photos/wekala/**` (جديد، 54 ملف: 52 منتج + شعار + غلاف)، `app.js`, `store-slugs.js`, `index.html`, `sw.js`, `CLAUDE.md` + صف `stores` id 90 وصفوف `products` 1720001-1720052 في Supabase | نعم — `npm run check:versions` ناجح. **لم يُنفَّذ `git commit`/push بعد — بانتظار تعليمات المستخدم. مهمة مفتوحة لجلسة قادمة: تحديث `stores.lat/lng`/`map_url`/`address` بالإحداثيات الدقيقة حين يرسلها المستخدم — تذكّر فحص هل يحتاج تعديل `wekala-data.js` مباشرة أيضاً (وليس Supabase فقط) حسب درس location/mapUrl الموثَّق في قسم 6** |
| 2026-07-13 | المستخدم طلب إضافة مطعم «بيت بيروت» (Beit Beyrut) الجديد ضمن قسم مطاعم، معطياً رابط قائمة إلكترونية (`qrlist.app/en/beitbeyrutgn`) + عنوان (Kayabaşı, İsimsiz Yol, Başakşehir/İstanbul) + هاتف (+90 533 130 17 31) | ميزة إضافة جديدة (لا يوجد خطأ سابق) — أول مصدر **qrlist.app** لهذا المشروع لا يُبنى على منهجية `mandishebam`/`albaraa-kunafa` القديمة (`__NUXT_DATA__` فقط)؛ الصفحة الرئيسية تعرض فقط 14 صنفاً «الأكثر طلباً»، والمنتجات في `__NUXT_DATA__` كلها تحت `definition_id:1` واحد بلا ربط حقيقي بالفئات — لو اعتُمد الاستنتاج اليدوي للفئات (كما فعلت جلسات qrlist السابقة الموثَّقة في [[adding-a-restaurant]]) كان سيفوت 52 صنفاً من أصل 66 | اكتُشف أن الموقع يعرض REST مفتوحاً غير موثَّق في `__NUXT_DATA__`: `GET /api/material?store_id=93&category_id=<id>&limit=100&app_locale=en` يُرجع **الفئة الحقيقية لكل صنف** (تحقّق عبر `fetch` من كونسول الصفحة نفسها بعد فحص طلبات الشبكة) — كشف قائمة كاملة 66 منتجاً عبر 7 فئات حقيقية (مناقيش 21، معجنات 5، فول وحمص 17، فلافل 5، مقبلات 10، مشروبات 6، عصائر طازجة 2) بدل الـ14 صنفاً الظاهرة في الصفحة الرئيسية فقط. متجر Kayaşehir/Beit Beyrut (`store_id=93` في qrlist) — إحداثيات المصدر (41.119112, 28.7714102) طابقت تماماً بحث Google Places (`findplacefromtext` بالاسم + `locationbias` حول الإحداثيات نفسها)، فاستُخدمت بيانات Google الحقيقية (تقييم 4.8 من 1807 تقييماً، ساعات عمل يومية موحّدة 9ص-9م، عنوان أدق من عنوان المستخدم النصي) بدل التخمين. المتجر رقم **89** (بعد التحقق من `max(id)` الفعلي في Supabase = 88 مرتين — مرة أولى وأخرى قبل التنفيذ مباشرة، طبقاً لدرس تصادم المعرّفات في [[adding-a-restaurant]])، حزمة معرّفات المنتجات **1710001–1710066** (بعد التحقق أن لا صفوف `products` موجودة أصلاً في هذا النطاق). صور الـ66 منتجاً + الشعار + 3 صور سلايدر حُمِّلت مباشرة (لا تكرار لمسارات الصور — تحقّق برمجي: 66 مساراً فريداً)، وصُغِّرت بـ`sharp` (منتجات ≤800px JPEG q82، شعار ≤400px PNG، غلاف مُختار من صور السلايدر الثلاث ≤1280px JPEG q82 — اختير السلايدر الذي يُظهر طبقي فول وحمص حقيقيين مع اسم العلامة وموقعها). أُنشئ `beitbeyrut-data.js` (نمط `alnoor-data.js`: `beitbeyrutStore` + `beitbeyrutFullCatalog` الكامل للدفع فقط + `beitbeyrutProductCatalog=[]` فارغ في المستودع للأداء). **اكتشاف حالة متزامنة قبل البدء:** `index.html`/`sw.js`/`store-slugs.js` كانت تحمل تعديلات غير محفوظة سابقة من جلسة أخرى تُخفِّض أرقام إصدار `app.js`/`styles.css`/`CACHE` عن قيم `HEAD` الفعلية (`origin/main` كانت متطابقة تماماً مع `HEAD` المحلي، فالتخفيض كان خطأ صريحاً وليس عملاً متقدماً) بينما تُضيف بالمقابل سطراً صحيحاً وناقصاً فعلاً من `main` (`88: "fistik-halep-sweets"` في `store-slugs.js`، لمتجر مدفوع مسبقاً لكن التزامه لم يصل بعد) — عُزل هذا عبر `git stash` للملفات الثلاثة، ثم أُعيد فقط سطر الـslug الناقص يدوياً فوق `HEAD` النظيف قبل البدء بالوصل الخاص بي، دون إعادة تخفيض الإصدارات. وُصلت الأسلاك الأربعة الإلزامية: `app.js` (`stores.push`, `products.push`, `initialDeliverySettings` spread) + `store-slugs.js` (`89: "beit-beyrut-restaurant"`) + `<script src="/beitbeyrut-data.js?v=1">` في `index.html` قبل `app.js` + إدراج مماثل في `sw.js` (`APP_SHELL`). **الدفع لقاعدة البيانات:** عبر Supabase MCP `execute_sql` (privileged؛ anon-key writes محجوبة بـRLS) — `jsonb_to_recordset` لصف المتجر (مع `approval_status:'approved'` مباشرة لأن الصور نُشرت محلياً أولاً، بالإضافة إلى `google_place_id/google_rating/google_reviews_count/google_maps_url` من بحث Google Places أعلاه) وصفوف الـ66 منتجاً معاً في معاملة منفصلة. تحقّق شامل عبر preview محلي (منفذ منفصل تلقائي): `/store/beit-beyrut-restaurant` يعرض العنوان الصحيح (`دكانجي - بيت بيروت`)، الوصف، الشعار والغلاف (HTTP 200 لكليهما)، كل الفئات السبع بعناصرها وأسعارها الصحيحة من Supabase (66/66، البيانات المحمَّلة محلياً فارغة عمداً فهذا يثبت أن القراءة من القاعدة تعمل)، `/category/restaurants` يعرض المتجر ضمن قائمة الـ40 متجراً بحالة «مفتوح» و«٢٠ ل.ت/كم»، صفر أخطاء كونسول. رُفع الإصدار: `app.js` v296→297، `store-slugs.js` v238→239، `CACHE` v214→215 في `sw.js`، و`npm run check:versions` ناجح | `beitbeyrut-data.js` (جديد)، `assets/photos/beitbeyrut/**` (جديد، 68 ملف)، `app.js`, `store-slugs.js`, `index.html`, `sw.js`, `CLAUDE.md` + صف `stores` id 89 وصفوف `products` 1710001-1710066 في Supabase | نعم — `npm run check:versions` ناجح. **لم يُنفَّذ `git commit`/push بعد — بانتظار تعليمات المستخدم** |
| 2026-07-13 | المستخدم طلب حذف متجرين بالكامل مع أي ارتباط لهما في أي صفحة: `8dec-coffee` (id 82، 8 DEC COFFEE) و`sham-grill-restaurant` (id 73، مطعم شام غريل) | ليس خطأ — طلب حذف مباشر. كلا المتجرين من فئة "المدموجة يدوياً" (bundled `<slug>-data.js` محمَّل عبر `<script>` في `index.html` ومُدمَج في `app.js` عبر `stores.push`/`products.push`/`initialDeliverySettings` spread)، فحذف صف Supabase وحده لا يكفي (نفس فئة الخطأ الموثَّقة في قسم 6) | تحقّق أولاً عبر Supabase أن لا طلبات (`orders`) أو مستخدمي متجر (`store_users`) أو تقييمات مرتبطة بالمتجرين (فقط `products` و`store_credentials`، آمن للحذف الكامل بلا فقدان بيانات تشغيلية). حذف كامل: (1) Supabase — `products`/`store_credentials`/`stores` لِلمعرّفين 73 و82. (2) `app.js` — إزالة `shamgrillStore`/`dec8Store` من `stores.push(...)`، وسطرا `products.push(...shamgrillProducts)`/`products.push(...dec8Products)`، وسطرا `...shamgrillDeliverySettings`/`...dec8DeliverySettings` من `initialDeliverySettings`. (3) حذف `shamgrill-data.js` و`8dec-data.js` بالكامل + مجلدي `assets/photos/shamgrill/` و`assets/photos/8dec/`. (4) إزالة وسمي `<script>` المقابلين من `index.html`. (5) إزالة الإدخالين (`73`, `82`) من `store-slugs.js`. (6) `sw.js`: الملفان لم يكونا مُدرَجين أصلاً في `APP_SHELL` فلا حاجة لإزالة سطر منه. تحقّق شامل عبر preview محلي: `/store/sham-grill-restaurant` و`/store/8dec-coffee` يعيدان توجيه للصفحة الرئيسية (لا صفحة متجر ولا خطأ)، `stores`/`products` العالميان في المتصفح خاليان تماماً من المعرّفين 73/82، `/category/restaurants` يحمّل بلا أخطاء كونسول، وSupabase تؤكد صفر صفوف متبقية لهما | `app.js`, `index.html`, `sw.js`, `store-slugs.js`, حذف `shamgrill-data.js`، `8dec-data.js`، `assets/photos/shamgrill/**`، `assets/photos/8dec/**`, `CLAUDE.md` + حذف صفَّي `stores` id 73/82 وصفوف `products`/`store_credentials` المرتبطة في Supabase | نعم — `npm run check:versions` ناجح. **ملاحظة تزامن:** جلسة أخرى كانت قد دفعت التزامات لاحقة على نفس الفرع أثناء العمل (app.js وصل v294 وCACHE v212 على origin/main)، فأُعيد بناء رقم الإصدار بالاستناد لـHEAD الفعلي وقت الالتزام لا للقيمة التي بدأت منها الجلسة: app.js v294→v295، `store-slugs.js` v237→v238، CACHE v212→v213. عُزلت تعديلاتي عبر `git apply --cached --unidiff-zero` عن تعديل غير ذي صلة موجود مسبقاً في نفس الملفات (إضافة متجر 88 فستق حلب في `store-slugs.js` لم تُلمس، وبقيت غير مرحّلة كما كانت) |
| 2026-07-12 | المستخدم طلب مطابقة أوقات الفتح/الإغلاق لكل المتاجر مع غوغل مابس والتأكد أن المعلومة ظاهرة على صفحة كل متجر — متابعة/تدقيق لمهاجرة `hours` الأصلية بتاريخ 2026-07-10 (تحت سطر منفصل في هذا السجل) بعد إضافة 3 متاجر جديدة (86 فلافل النور، 87 فلوكا، 88 فستق حلب) وتغيّر بيانات Google الحية لبعض الفروع خلال اليومين | لا خطأ برمجي — تدقيق بيانات دوري. أهم اكتشاف: `mapDbStore()`/`loadCatalogFromSupabase()` في `app.js` (سطر ~613/693) يعيدان بناء `stores` بالكامل من Supabase (`hours: r.hours` مباشرة) في كل تحميل صفحة، فحقل الساعات **لا** يتأثر بفئة «الشادوينغ» الموثَّقة في قسم 6 لحقلي location/mapUrl على المتاجر المدمَجة يدوياً (`*-data.js`) — لا حاجة لتعديل أي ملف `<slug>-data.js` لهذه المهمة تحديداً، خلافاً لما قد يُفترض قياساً على سوابق location/mapUrl | كتبت سكربت مؤقت (scratchpad، بنفس نمط `scripts/update-google-ratings.js` غير الملتزم بالمستودع، حسب سابقة 2026-07-10) يجلب `opening_hours.weekday_text` عبر Google Places (يُفضّل `google_place_id` المحفوظ مسبقاً من مهاجرة التقييمات كتطابق موثوق دون إعادة بحث، وإلا Nearby/FindPlace بنفس حارس المسافة 450م)، ويحوّلها لنفس الصيغة العربية المستخدمة أصلاً في القاعدة («يومياً HH:MM–HH:MM (يوم استثناء)»). فحص كل الـ77 متجراً غير المرفوضة: 61 مطابقة بالفعل (لا تغيير)، 11 بلا تطابق موثوق على غوغل (تُركت كما هي — الحقل يبقى غير فارغ بنص واقعي أو دعوة صريحة للتأكيد عبر واتساب/الاتصال، وهذا يفي بمطلب «المعلومة موجودة» حتى دون رقم غوغل رسمي)، و5 حُدِّثت فعلياً بعد تحقّق يدوي إضافي (اسم/عنوان/هاتف مطابق حرفياً قبل الكتابة، وليس فقط نطاق 450م): #25 بايت هاوس (تغيّر إغلاق الثلاثاء الفعلي)، #42 زيتونة أوغلو (بيانات غوغل الحية توحّدت لساعات يومية واحدة، الاستثناء القديم لم يعد صحيحاً)، #86 فلافل النور (توحيد صيغة الشرطة فقط)، #87 فلوكا (أول تطابق غوغل موثوق منذ الإضافة — كان بلا `google_place_id`)، #88 فستق حلب (تطابق دقيق بالاسم/العنوان). **إضافة يدوية بعد الفشل التلقائي:** #84 ملحمة الدوماني و#50 صفا الشام رفضهما السكربت الآلي (بحث بالاسم فقط ضمن نطاق 450م) فأُعيد البحث بنص حر (عنوان الشارع/اسم بديل) خارج حارس السكربت، وتحقّقتُ يدوياً قبل الكتابة: #84 مطابقة عنوان حرفية 100% (نفس رقم المبنى/الوحدة) **و**نفس رقم هاتف المتجر بالضبط رغم بُعد 464م (فوق حد الـ450م الآلي — الإحداثيات المخزَّنة للمتجر تبدو غير دقيقة، لا الموقع الحقيقي) فحُدِّثت ساعاته يدوياً («يومياً 9:00 ص – 9:00 م (الأربعاء مغلق)»)؛ #50 مطابقة بمسافة 17 متراً فقط لكن الساعات المكتشفة (يومياً 10ص-11م) مطابقة بالفعل لما كان مخزَّناً فلم يتغيّر شيء عملياً. كلا المتجرين + #87 و#88 حصلا أيضاً على `google_place_id` محفوظ الآن (كان فارغاً) كأثر جانبي مفيد لأي مزامنة تقييمات مستقبلية. تحقّق حي عبر preview محلي (منفذ منفصل تلقائي، تعارض مع خادم جلسة أخرى كانت تعمل بالتوازي على 4173): صفحتا `/store/domani` و`/store/25` (بايت هاوس) عرضتا النص الجديد بالضبط في «أوقات العمل»، وشارة «مغلق الآن/مفتوح الآن» فسّرت الاستثناء الجديد («الأربعاء مغلق») بشكل صحيح بفضل التوافق التام لصيغة النص مع `storeHoursStatus()`/`HOURS_DAY_NAMES` الموجودة أصلاً في `app.js`؛ لوحظ أن قراءة الحقل قبل اكتمال `loadCatalogFromSupabase()` (تحميل مؤجَّل للكتالوج الكامل) تُظهر مؤقتاً القيمة القديمة من الحمولة الأولية إلى حين اكتمال التحميل الكامل — سلوك متوقَّع وليس عطلاً. صفر أخطاء كونسول | لا شيء في كود المستودع (Supabase فقط: عمود `stores.hours` لـ6 متاجر + `stores.google_place_id` لـ4 متاجر عبر `execute_sql`)، `CLAUDE.md` | لا ينطبق (لم يُعدَّل `app.js`/`styles.css`/`index.html`/`sw.js`) |
| 2026-07-12 | المستخدم طلب إنشاء متجر كامل على دكانجي من كتالوج واتساب بزنس لمتجر منافس/طرف ثالث («حلويات فستق حلب صالة ميدان إسنيورت»، +90 555 101 12 22) — شعار + غلاف + 74 منتجاً بتصنيفات ووصوفات وأسعار وصور حقيقية «كما وردت» | ميزة إضافة جديدة (لا خطأ سابق)، لكن اعترضتها 3 مشاكل تقنية جوهرية: (1) كتالوج واتساب يُحمَّل بتصفّح شبكي تدريجي (لا فرز حقيقي) — عدة محاولات توقّف ظاهرياً عند "النهاية" (74 عنصراً نصياً) كانت في الحقيقة سقوفاً مؤقتة تتجاوزها فقط أحداث تمرير **حقيقية** (عبر أداة `computer.scroll`، أي CDP Input حقيقي) — `element.scrollTop=` أو `dispatchEvent(WheelEvent)` البرمجي كان يتوقف عند سقف ثابت لا يتخطّاه أبداً رغم آلاف المحاولات، فاكتُشف بعدها أن العدد الحقيقي **74 وليس أقل**. (2) صور المنتجات مخزَّنة كـ`blob:` (مرتبطة بجلسة المتصفح فقط، لا رابط عام) — استُخرجت بإجبار تحميل كل صورة (`fetch(blobUrl)→anchor[download]→click()`) لمجلد التنزيلات المحلي. (3) **مطابقة الاسم+السعر كمفتاح لمنع التكرار فشلت بصمت** لأن نص واتساب يحوي فراغاً غير منقسم (NBSP) بدل الفراغ العادي في بعض التكرارات (بين "TRY" والرقم) — فشلت مقارنة النصوص المعاد كتابتها يدوياً، فأُعيد بناء الفهرسة الصحيحة عبر **مقارنة MD5 لملفات الصور الفعلية** (116 تحميلاً → 74 هاش فريد بالضبط، مطابق تماماً لعدد المنتجات الحقيقي) بدل الاعتماد على نص واتساب كمفتاح وحيد | استُخدم حساب Chrome المرتبط الفعلي (طلب المستخدم صراحة) عبر `mcp__claude-in-chrome__*` لتسجيل الدخول لواتساب ويب وفتح الكتالوج (بيانات عامة قابلة للمشاهدة من أي زائر، وليست بيانات اعتماد أو محتوى خاص). صُغِّرت 74 صورة منتج + الشعار + غلاف (أول صورة "كعك العيد" ذات تصميم إعلاني جميل استُخدمت كغلاف) بـ`sharp` (webp q82، ≤800px منتجات / ≤1280px غلاف) ورُفعت لباكت `campaign-images` في Supabase Storage (لا حاجة نشر Vercel، ظاهرة فوراً). كُتب وصف عربي قصير أصلي لكل من الـ74 منتجاً (ليس نصاً مُخترعاً كذباً، بل وصف عام صحيح للصنف كحلوى شامية تقليدية معروفة) وصُنِّفت ضمن 11 فئة فرعية منطقية مطابقة لتنظيم الكتالوج المصدر مع دمج الأصناف غير المصنَّفة أصلاً (نواشف و معمول، بيتي فور، حلو عربي بالفستق الحلبي، علب حلو عربي مشكل، قشاطي، تسالي، قهوة، راحة الحلقوم، شوكولاته، هريسة اللوز، ضيافة و مناسبات). العنوان/الإحداثيات أُخذت من ملف تعريف واتساب بزنس الحقيقي (`Bağlarçeşme, 1167. Sk. No:19, Esenyurt/İstanbul`) وحُوِّلت عبر Google Geocoding API الحي (`api/maps-key`) لإحداثيات دقة ROOFTOP (41.034965, 28.675828) — لا تخمين. **🔴 تصادم معرّفات حي أثناء التنفيذ:** المعرّف المخطَّط أصلاً (86، بحسب فحص أولي) أصبح مأخوذاً **مرتين** بجلستين متزامنتين أضافتا «فلافل النور» (86) ثم «مطعم أسماك فلوكا» (87) بينما كنت أجمّع الصور — أُعيد فحص `max(id)`/تعارض الحزمة عبر `execute_sql` **مباشرة قبل التنفيذ** (وليس بالاعتماد على فحص أول الجلسة) فاكتُشف التصادم وتحوَّل المعرّف لـ88 وحزمة المنتجات لـ1700001 قبل أي `INSERT`. الدفع عبر `execute_sql` (privileged، `jsonb_to_recordset`) بمعاملة واحدة للمتجر + 74 منتجاً. `approval_status='pending'` عمداً (هذا متجر طرف ثالث حقيقي وليس شراكة مؤكَّدة مع دكانجي — لا اعتماد تلقائي بلا تفويض صريح إضافي). أُضيف `88: "fistik-halep-sweets"` في `store-slugs.js` **فقط** (سطر واحد معزول وسط نطاق تعديل ضخم غير ذي صلة من جلسات متزامنة أخرى — 69 ملفاً معدَّلاً وقتها في `git status`، لم يُلمس أي منها). تحقّق شامل عبر preview محلي بمنفذ مستقل (`autoPort`، لتفادي تعارض مع خادم جلسة أخرى كان يعمل بالتوازي على 4173): العنوان والوصف والشعار (400px) والغلاف (720px) والـ74 منتجاً بفئاتها وأسعارها الصحيحة (بما فيها منتج واحد `price_on_request` بلا سعر معروض في المصدر) ظهرت جميعها بلا أي خطأ كونسول. **لم يُنفَّذ `git commit`/push** (فقط تعديل ملف واحد غير مرحّل، بانتظار تعليمات المستخدم؛ ملاحظة: الغلاف/الشعار/صور المنتجات على Supabase Storage فليست بحاجة نشر Vercel أصلاً، فالمتجر ظاهر بالكامل بصوره حياً فوراً على `/store/88` رغم ذلك) | لا شيء في كود المستودع سوى سطر واحد في `store-slugs.js` (غير مرحّل)، `CLAUDE.md` + صف `stores` id 88 وصفوف `products` 1700001-1700074 + 76 ملف صورة (شعار+غلاف+74 منتجاً) في Supabase Storage | لا ينطبق على الصور (Storage مباشر بلا نشر)؛ `store-slugs.js` بحاجة نشر منفصل لاحقاً لتفعيل الرابط النظيف `/store/fistik-halep-sweets` (الرقمي `/store/88` يعمل حياً فوراً بفضل إصلاح `hydratePageData` الموثَّق أعلاه بتاريخ 2026-07-08) |
| 2026-07-12 | المستخدم طلب رفع سعر التوصيل حسب المسافة لكل متاجر «مطاعم» من 15 إلى 20 ل.ت/كم (بعد نشر متجر فلافل النور، إشارةً للسطر أعلاه في نفس اليوم) | ميزة/سياسة تسعير جديدة (لا خطأ سابق)، لكن التنفيذ اصطدم بحقيقتين معماريتين موثَّقتين في [[delivery-settings-localstorage]]: (1) `ratePerKm` **ليس** في Supabase إطلاقاً — يعيش فقط داخل كل `<slug>DeliverySettings` في الملف المُجمَّع + يُخزَّن مُظلَّلاً في `localStorage` لكل متصفح عبر `saveState()`، فتعديل الملفات وحدها لا يكفي للزوار العائدين (نفس فئة خطأ `khawali40` الموثَّقة أصلاً في `runDeliveryMigrations()`). (2) متجر الخوالي (31) كان له استثناء مقصود سابقاً بـ40 ل.ت/كم (وليس 15 كباقي المطاعم) — قرار المستخدم الصريح «لكل المطاعم» فُهم كتوحيد شامل يشمله أيضاً | حُدِّدت 45 متجراً بفئة `مطاعم` عبر استعلام Supabase مباشر (وليس تخميناً من الذاكرة أو الملفات) — استبعد بذلك متجر id 72 (حضرموت اليمن) رغم ظهوره في القائمة المحلية: صفّه **محذوف فعلياً من Supabase** منذ تدقيق 2026-07-10 لكن `hadramout-data.js` لا يزال محمَّلاً كملف ميت (نفس فئة خطأ قسم 6 «حذف متجر لا يكفي وحده») — لم يُعدَّل لأنه لا يظهر أصلاً على الموقع الحي (تحقّق: `/store/72` يُرجع الصفحة الرئيسية العامة لا صفحة متجر). عُدِّل `ratePerKm` من 15 (أو 40 لحالة الخوالي) إلى 20 في 38 ملف `<slug>-data.js` (بينها 3 ملفات متعددة الفروع alagar/anas/wingi تُحدَّث بسطر قالب واحد يغطي كل فروعها). أُضيف مهاجرة جديدة `restaurantRate20` في `runDeliveryMigrations()` (نفس نمط `khawali40`) تفرض `ratePerKm=20` على أي نسخة `localStorage` سابقة محفوظة للـ45 معرّفاً، بلا انتظار انتهاء صلاحية التخزين المؤقت. رُفعت أرقام إصدار كل ملف من الـ38 (+1 لكل واحد) في `index.html` وما يقابله في `sw.js` (17 ملفاً منها فقط مُدرَجة أصلاً في `APP_SHELL`)، ورُفع `CACHE` مرة إضافية لضمان وصول كود المهاجرة الجديد. **حادثة تزامن حادة أثناء التنفيذ:** جلسة أخرى كانت تعمل بالتوازي على نفس `app.js`/`index.html`/`sw.js`/`store-slugs.js` (ميزة الذكاء الاصطناعي + إضافة متجر فلوكا 87، أنهت والتزمت الأولى فعلاً منتصف هذه المهمة) — عُزلت تعديلاتي بدقة عبر `git apply --cached --unidiff-zero` على ررقعات مبنية يدوياً (فرق كل ملف بصفر سياق `-U0` لتفكيك كل سطر تعديل كهنك مستقل، ثم تصفيتها بكلمات مفتاحية لاستبعاد أسطر فلوكا/store-slugs.js تحديداً) بدل `git add` كامل للملفات المشتركة؛ تحقّق نهائي عبر `git diff --cached`/`git diff` أن المرحلتين منفصلتان بدقة 100%. تحقّق شامل عبر preview محلي (منفذ منفصل): محاكاة زائر عائد بقيمة `localStorage` قديمة (15 لمتجر 17، 40 لمتجر الخوالي 31) ثم إعادة تحميل الصفحة أظهرت كلا القيمتين محدَّثتين فعلياً إلى 20 بعد المهاجرة، صفحات `/category/restaurants` وصفحة متجر الخوالي الفردية تعرضان «٢٠ ل.ت/كم» بدل «١٥»/«٤٠» بلا أي أخطاء كونسول، و`npm run check:versions` ناجح | 38 ملف `<slug>-data.js` (قائمة كاملة في هذا السطر)، `app.js` (مهاجرة جديدة فقط)، `index.html`, `sw.js`, `CLAUDE.md` | نعم — `npm run check:versions` ناجح. **لم يُنفَّذ `git commit`/push بعد — بانتظار تعليمات المستخدم** |
| 2026-07-12 | المستخدم طلب إضافة مطعم «مطعم أسماك فلوكا» (Feluka Seafood Restaurant) الجديد ضمن قسم مطاعم، معطياً رابط قائمة إلكترونية (`feluka.minidine.com/ar/categories/?branch=feluka-seafood-restaurant`) + هاتف (+90 537 047 71 10) + Plus Code (3X9P+523 Şişli, İstanbul) | ميزة إضافة جديدة (لا يوجد خطأ سابق) — أول مصدر من نوع **minidine.com/miniandmore.co الجديد** (نطاق white-label جديد، وليس نفس نمط bitehaus/ruman المُوثَّق سابقاً بنفس العائلة) | **مصدر جديد — feluka.minidine.com (Next.js/Turbopack SPA فوق auth0.miniandmore.co):** الواجهة تُخفي القائمة خلف طلب تبادل توكن أولاً؛ استُخرج المسار من حزم JS المصغَّرة (`_next/static/chunks/*.js`، بحث عن `customerName`/`clients/token`): `POST https://auth0.miniandmore.co/clients/token` بجسم **multipart form-data** (وليس urlencoded) يحوي `customer_name` (بشرطة سفلية **وليس** شرطة — القيمة الفعلية `feluka_seafood_restaurant` مختلفة عن slug الرابط `feluka-seafood-restaurant` الذي تظهر به الصفحة، اكتُشف بالفحص الدقيق لملف JS بعد فشل أول محاولتين: أولاً "the origin url field is required" ثم "Customer not found") + `service=qrmenu` + `origin_url` (رابط الموقع نفسه) → يُعيد `{access_token, url, storage_url}`؛ ثم `GET {url}/categories` و`/menu/items` و`/contact_info` بترويسة `Authorization: Bearer <token>` و`X-Data-Lang: ar` تُعيد كل شيء (19 تصنيفاً، 80 عنصراً، 79 منها بصورة فريدة — عنصر واحد بلا صورة استُبعد). صور العناصر على S3: `{storage_url}/feluka_seafood_restaurant/items/<name>.<ext>` (بلا مصادقة، لكن حساسة لمسار خاطئ: أول محاولة بنَت الاسم بامتداد مكرَّر `<name>.<ext>.<ext>` فأعادت 403 AccessDenied لكل الملفات، صُحِّح بإعادة بناء الاسم من `image.image` مباشرة الذي يتضمّن الامتداد أصلاً). بعض العناصر (25/80) تحمل `attributes[]` وصفية (حجم/نوع) لكنها **ليست خيارات سعر بديلة** — كل عنصر بهذه الصفة له سجل واحد فقط بلا شقيق بسعر مختلف، فأُلحقت كنص وصفي بالاسم («- إكسترا»/«- كبير») بدل بناء مجموعة خيارات وهمية، مع تصفية القيم المكرَّرة أصلاً داخل الاسم (كـ«جمبري مشوي كبير - كبير») لتفادي التكرار. إحداثيات المتجر مأخوذة مباشرة من رابط iframe خرائط غوغل المُضمَّن في استجابة `/contact_info` (`embed_map_src`، 41.067719911170165 / 28.985088271163935) لا حاجة لبحث Places منفصل — طابقت منطقة الـPlus Code التي أرسلها المستخدم (شيشلي) تماماً. الشعار المُنزَّل من `feluka.minidine.com/logo.png` تبيَّن أنه **الشعار الفعلي للعلامة التجارية** (دفة سفينة + سمكة + صياد، نص "FELUKA RESTAURANT") وليس شعاراً عاماً للمنصة كما بدا مبدئياً — استُخدم مباشرة بلا حاجة لصورة بديلة. **المتجر رقم 87** (بعد التحقق من `max(id)` الفعلي في Supabase = 86 وليس أي رقم من الذاكرة أو الملف الحالي لفرع العمل، لأن متجر فلافل النور 86 كان قد دُفع لقاعدة البيانات فعلاً في جلسة سابقة رغم بقاء `git commit` معلَّقاً)، حزمة معرّفات المنتجات **1690001-1690079**. أُنشئ `feluka-data.js` (نفس نمط `alnoor-data.js`: `felukaStore` + `felukaFullCatalog` الكامل للدفع فقط + `felukaProductCatalog=[]` فارغ في المستودع للأداء) + صور محمَّلة محلياً (`assets/photos/feluka/`: شعار PNG + غلاف JPG (لقطة صينية سي فود كاملة بديكور بحري — قوارب وأصداف تطابق هوية العلامة) + 79 صورة صنف مضغوطة بـ`sharp` إلى ≤1000px JPEG q82، إجمالي ~6.6MB). وُصلت الأسلاك الأربعة الإلزامية في `app.js` (`stores.push`, `products.push`, `initialDeliverySettings` spread) + `store-slugs.js` (`87: "feluka-seafood-restaurant"`) + `<script src="/feluka-data.js?v=1">` في `index.html` قبل `app.js` + إدراج مماثل في `sw.js` (`APP_SHELL`). **الدفع لقاعدة البيانات:** عبر Supabase MCP `execute_sql` (privileged) — `jsonb_to_recordset` لصف المتجر وصفوف الـ79 منتجاً في معاملتين منفصلتين؛ **خطأ نسخ يدوي واحد اكتُشف وأُصلح فوراً بالتحقق البرمجي** (مقارنة كل صف بالمصدر الأصلي): معرّف صورة منتج «ورق عنب (مقلي) فردين» فُقدت منه أرقام أثناء كتابة استعلام SQL يدوياً («6111777694» بدل «6111767777694»)؛ صُحِّح بـ`UPDATE` مباشر ثم أُعيد التحقق البرمجي الكامل (79/79 صف مطابق تماماً للمصدر: الاسم، السعر، التصنيف، مسار الصورة) وتأكيد وجود كل ملفات الصور الـ79 فعلياً على القرص. **approval_status = 'pending' عمداً** (خلافاً لسطر فلافل النور أعلاه الذي اعتُمد مباشرة) — الصور لم تُنشر بعد لأن هذه الجلسة لم تُنفِّذ `git commit`/push، فاعتماد المتجر الآن كان سيُظهره على الموقع الحي بصور مكسورة (نفس التحذير الموثَّق في [[adding-a-restaurant]]: "انتظر نشر الصور قبل الاعتماد"). **حادثة تزامن أثناء التنفيذ:** جلسة أخرى كانت تعمل بالتوازي على نفس الملفات (`app.js`/`index.html`/`sw.js`/`store-slugs.js`) وأنهت والتزمت (commit) إضافة فلافل النور منتصف هذه الجلسة، مما **حذف سطر `store-slugs.js` (87) الذي كنت أضفته للتو** (استُرجع فوراً بعد ملاحظة `system-reminder` تلقائي عن تعديل خارجي على الملف) ورفعت أرقام إصدار عدة ملفات `*-data.js` أخرى في `sw.js` لا علاقة لها بهذه المهمة — لم تُلمس تلك التعديلات، واقتصر التدخل على استعادة سطري الخاص فقط. تحقّق شامل عبر preview محلي (منفذ منفصل لتفادي تعارض مع خادم الجلسة الأخرى): `/store/feluka-seafood-restaurant` يعرض العنوان الصحيح، الوصف، الشعار والغلاف (HTTP 200 لكليهما)، كل التصنيفات الـ19 بعناصرها الصحيحة (79 منتجاً)، حقل «أوقات العمل» (لا وجود لساعات حقيقية في المصدر، فاستُخدمت الصيغة القياسية «يومياً — يرجى التأكيد عبر الاتصال» المتَّبعة في متاجر أخرى بلا ساعات مصدر بدل ترك الحقل `null` الذي كان سيظهر حرفياً في الواجهة)، صفر أخطاء كونسول، و`/` و`/category/restaurants` بلا أي تأثر. رُفع الإصدار: `app.js` v284→285، `store-slugs.js` v236→237، `CACHE` v202→203 في `sw.js`، و`npm run check:versions` ناجح | `feluka-data.js` (جديد)، `assets/photos/feluka/**` (جديد، 81 ملف)، `app.js`, `store-slugs.js`, `index.html`, `sw.js`, `CLAUDE.md` + صف `stores` id 87 وصفوف `products` 1690001-1690079 في Supabase | نعم — `npm run check:versions` ناجح. **لم يُنفَّذ `git commit`/push بعد، والمتجر لا يزال `pending` (غير ظاهر على الموقع الحي) بانتظار تعليمات المستخدم للنشر** |
| 2026-07-12 | المستخدم طلب إضافة مطعم «فلافل النور» (Al Noor Falafel) الجديد ضمن قسم مطاعم، معطياً رابط قائمة إلكترونية (`menu.akillikurye.com/AlNoor-Restaurant`) + عنوان (Hacı Bayram Veli Caddesi 87B، مهترچشمة، إسنيورت) + هاتف | ميزة إضافة جديدة (لا يوجد خطأ سابق) — أول مصدر من نوع **akillikurye.com** لهذا المشروع | **مصدر جديد — akillikurye.com (Angular SPA):** الواجهة تُخفي البيانات خلف Angular، لكن REST بسيط مكشوف: base URL يُستخرج من حزمة `main-*.js` (`Cn.domin` = `https://backend.akillikurye.com/`)، ثم `GET {domin}api/menu/of/{slug}` يُعيد كل شيء دفعة واحدة (JSON خام بلا مصادقة): بيانات المتجر (`lat/lng/address/logo/banner` كمسارات نسبية تحت `{domin}storage/`) + `categories[].items[]` (كل عنصر `name.{ar,en,tr}`, `price`, `photo`, `sizes[]`, `addons[]`). لا خيارات/إضافات هنا (65 منتجاً بسيطة، صورة فريدة لكل عنصر). إحداثيات المصدر (41.0213625, 28.6648594) تطابقت شبه حرفياً (~3م فرق) مع نتيجة Google Places (`findplacefromtext` بالاسم+نطاق قريب من الإحداثيات) على "مطعم النور" (rating 4.4، 130 تقييم، ساعات عمل يومية 9ص-10م) — استُخدمت ساعات Google الحقيقية بدل تخمين نص عام. **المتجر رقم 86** (بعد التحقق من `max(id)` الفعلي في Supabase = 85 وليس أي رقم من الذاكرة)، حزمة معرّفات المنتجات **1680001-1680065** (بعد التحقق أن أعلى معرّف منتج مستخدم فعلياً هو 1670111). أُنشئ `alnoor-data.js` (نمط `rody-data.js`/`saj-data.js`: `alnoorStore` + `alnoorFullCatalog` الكامل للدفع فقط + `alnoorProductCatalog=[]` فارغ في المستودع للأداء) + صور محمَّلة محلياً (`assets/photos/alnoor/`: شعار + غلاف + 65 صورة صنف، إجمالي 4MB، كل الملفات أقل من 260KB فرداً). وُصلت الأسلاك الأربعة الإلزامية في `app.js` (`stores.push`, `products.push`, `initialDeliverySettings` spread) + `store-slugs.js` (`86: "alnoor-falafel-restaurant"`) + `<script src="/alnoor-data.js?v=1">` في `index.html` قبل `app.js`. **الدفع لقاعدة البيانات:** عبر Supabase MCP `execute_sql` (privileged) — anon-key writes محجوبة بـRLS كما هو موثَّق — `jsonb_to_recordset` لصف المتجر (مع `approval_status:'approved'` مباشرة بما أن الصور نُشرت أولاً محلياً، بالإضافة إلى `google_place_id/google_rating/google_reviews_count/google_maps_url/google_rating_updated_at` من نفس بحث Google Places أعلاه) وصفوف الـ65 منتجاً معاً بمعاملة واحدة. تحقّق شامل عبر preview محلي: `/store/alnoor-falafel-restaurant` يعرض العنوان الصحيح، الوصف، الشعار والغلاف (HTTP 200 لكليهما)، كل الأصناف الخمسة (الأطباق ١٦، الأصناف ١١، السندويشات ١٢، الأطباق الخارجية ١٦، المشروبات ١٠ = ٦٥) بالأسعار الصحيحة من Supabase (البيانات المحمَّلة محلياً فارغة عمداً، فهذا يثبت أن القراءة من القاعدة تعمل)، صفر أخطاء كونسول، و`/category/restaurants` يعرض المتجر ضمن قائمة الـ٣٩ متجراً. رُفع الإصدار: `app.js` v282→283، `store-slugs.js` v235→236، `CACHE` v200→201 في `sw.js`، و`npm run check:versions` ناجح | `alnoor-data.js` (جديد)، `assets/photos/alnoor/**` (جديد، 67 ملف)، `app.js`, `store-slugs.js`, `index.html`, `sw.js`, `CLAUDE.md` + صف `stores` id 86 وصفوف `products` 1680001-1680065 في Supabase | نعم — `npm run check:versions` ناجح. **ملاحظة تزامن:** جلسة أخرى كانت تعمل بالتوازي على نفس `app.js`/`index.html`/`sw.js` (ميزة الذكاء الاصطناعي في «اسأل دكانجي» + رفع `styles.css`)، فعُزلت تعديلاتي عبر `git apply --cached` (تصحيحات دقيقة تستهدف فقط أسطر إضافة فلافل النور) بدل `git add` الكامل للملف، حفاظاً على عمل الجلسة الأخرى غير مؤكَّد الاستقرار بعد كتعديلات غير مرحّلة (staged) منفصلة. مدفوع لـ main بموافقة المستخدم الصريحة | `alnoor-data.js` (جديد)، `assets/photos/alnoor/**` (جديد، 67 ملف)، `app.js`, `store-slugs.js`, `index.html`, `sw.js`, `CLAUDE.md` + صف `stores` id 86 وصفوف `products` 1680001-1680065 في Supabase | نعم — `npm run check:versions` ناجح |
| 2026-07-12 | متابعة للسطر التالي أدناه: المستخدم طلب «صحّح إحداثيات متجر 17» بعد أن تبيّن في المراجعة اليدوية أنها مقرَّبة (41.004, 28.63) وتبعد ~2.4كم عن الموقع الحقيقي المؤكَّد (هاتف+عنوان مطابقان حرفياً) | **اكتشاف حرج أثناء التنفيذ:** تحديث `stores.lat/lng` في Supabase وحده **لم يظهر على الموقع إطلاقاً** — فحص `stores` العالمي في المتصفح بعد التحديث أظهر لا يزال القيم القديمة (41.004, 28.63) ومapUrl قديماً مختلفاً كلياً عمّا كُتب في القاعدة. السبب: متجر 17 من فئة المتاجر القديمة "المدموجة يدوياً" الموثَّقة في قسم 6 أعلاه (`afgan-data.js` محمَّل عبر `<script>` في `index.html` ومُدرَج في `sw.js`، ويُدمَج في `app.js` عبر `stores.push(afganStore)`) — حقلا `location`/`mapUrl` مصدرهما هذا الملف الثابت حصرياً وليس Supabase، فتحديث القاعدة وحده بلا لمس الملف كان سيبقى بلا أي أثر ظاهر إلى الأبد رغم نجاح استعلام SQL. **هذا امتداد جديد لنفس فئة الخطأ الموثَّقة في قسم 6 (ليس فقط "حذف متجر لا يكفي" بل أيضاً "تعديل حقل موقع/إحداثيات لا يكفي من Supabase وحده" لأي متجر من هذه الفئة)** | (1) صُحِّحت `stores.lat/lng` في Supabase إلى الإحداثيات المؤكَّدة (41.0192502, 28.6497621) — أُبقيت كسجل مرجعي لا كمصدر عرض فعلي لهذا المتجر تحديداً. (2) **الإصلاح الفعلي:** عُدِّل `afgan-data.js` مباشرة: `location.lat/lng` لنفس القيم المؤكَّدة، و`mapUrl` من رابط بحث نصي عام ("Afgan+Kebap+Restaurant+Beylikduzu...") إلى رابط إحداثيات دقيق (`?api=1&query=41.0192502,28.6497621`) بنفس نمط بقية المتاجر. رُفع `?v=126→127` في الموضعين (`index.html` وسطر `sw.js` ضمن `APP_SHELL`) لضمان كسر أي تخزين مؤقت قديم للملف. تحقّق كامل عبر preview محلي: `stores` العالمي في المتصفح بعد الإصلاح يحمل الإحداثيات الصحيحة الجديدة و`mapUrl` الجديد فوراً، `googleRating` (من الجلسة السابقة نفس اليوم) لا يزال سليماً 4.2، صفحة المتجر تُعرض بلا أي خطأ كونسول وبلا أي تغيّر بصري غير متوقَّع، و`npm run check:versions` ناجح | `afgan-data.js`, `index.html`, `sw.js`, `CLAUDE.md` + صف `stores` id 17 في Supabase | نعم (يدوياً بالبحث النصي؛ `check:versions` لا يتحقق من ملفات `*-data.js` الفردية، فقط app.js/styles.css) |
| 2026-07-12 | متابعة للسطر التالي أدناه: المستخدم طلب «حاول مراجعة id 17 و22 و36 و66 يدوياً» — الأربعة التي تركها السكربت الآلي بلا تقييم لأن مطابقتها الاسمية بدت مشبوهة | الأسباب الجذرية اختلفت لكل حالة (اكتُشفت بالتحقق اليدوي الفعلي، وليس تخميناً): (1) id 17 أفغان كباب — إحداثيات المتجر في القاعدة مقرَّبة لـ3 خانات عشرية فقط (41.004, 28.63) فتبعد ~2.4كم عن الموقع الحقيقي، فتجاوزت حارس الـ450م في السكربت الآلي رغم أن المكان صحيح 100%. (2) id 66 الكنافة الفلسطينية — لا سبب واضح، غالباً نفس نوع خطأ حارس المسافة أو ترجيح اسم بديل؛ تبيّن أن `stores.map_url` كان يحمل أصلاً رابط Google Maps صحيحاً 100% لم يستخدمه السكربت (يعمل بالإحداثيات فقط لا بـmap_url). (3) id 36 زيتونة باشاك شهير — يوجد مكان باسم مشابه صدفة "Zeytunaoğlu" على بعد 7 أمتار فقط من إحداثيات المتجر المخزَّنة (يفسّر كيف أنتج السكربت مطابقة قريبة incorrect)، لكنه عمل مختلف تماماً (هاتف مختلف كلياً +90 530 342 37 82 لا يطابق هاتف المتجر +90 505 567 90 00، ولا يحمل اسم "Zaitoune/زيتونة" كباقي فروع السلسلة السبعة المؤكَّدة). (4) id 22 القاضي ماركت — عنوان المتجر في القاعدة نفسه غامض أصلاً ("اسنيورت / بيليك دوزو" بلا شارع)، ولم يُعثر على أي نشاط تجاري باسم قريب من "قاضي/Kadı Market" ضمن نطاق معقول من إحداثياته — على الأرجح لا يملك صفحة Google Business أصلاً | **مراجعة يدوية فعلية لكل حالة عبر Places API مباشرة (find place/text search/place details) + خرائط Google، بمطابقة الهاتف والعنوان والإحداثيات وليس الاسم فقط:** (1) **id 17 ✅ حُدِّث** — AFGAN KEBAP مطعم رز بخاري، 4.2★ (1227 تقييم)، مؤكَّد بتطابق حرفي مزدوج: نفس رقم الهاتف بالضبط (+90 555 080 46 40) ونفس عنوان الشارع بالضبط (2066 Sk 17/A). (2) **id 66 ✅ حُدِّث** — Filistin Künefesi، 4.3★ (145 تقييم)، مؤكَّد بحلّ رابط `stores.map_url` الموجود أصلاً في القاعدة مباشرة (بدل البحث بالاسم) ثم تطابق الهاتف حرفياً (+90 530 102 11 36) والإحداثيات لسبع خانات عشرية بالضبط. (3) **id 36 ⏭ تُرك بلا تقييم عمداً** — "Zeytunaoğlu" القريب مرفوض رغم قرب الإحداثيات لأن الهاتف مختلف تماماً والاسم لا يطابق نمط تسمية السلسلة؛ لا وجود لفرع باسم "Zaitoune باشاك شهير" ضمن نتائج بحث نصي شامل لعلامة "Zaitoune" في نطاق 1.5كم. (4) **id 22 ⏭ تُرك بلا تقييم عمداً** — لا نتيجة موثوقة رغم 4 محاولات بحث مختلفة (نص عربي/إنجليزي، بحث بالهاتف، بحث قريب بكلمة مفتاحية)؛ يوافق قرار جلسة تدقيق العناوين 2026-07-10 السابقة برفض "Çağrı Market" لنفس السبب. **درس تشغيلي (بوابة صلاحيات):** مصنّف الصلاحيات حجب أول محاولتين (كتابة id 66، ثم حتى استعلام قراءة بحت لاحق) لأن طلب المستخدم الأصلي «راجع يدوياً» فُهم كطلب تحقّق وليس تفويضاً تلقائياً للكتابة؛ تم إيقاف العمل والسؤال عبر AskUserQuestion، فاختار المستخدم صراحة «طبّق 17، وأكمل التحقق من 22/36 واكتب ما يثبت بثقة عالية» — وبهذا التفويض المحدَّد استُؤنف العمل حتى الإكمال. **درس معماري:** السكربت الآلي (`scripts/update-google-ratings.js`) لا يستخدم `stores.map_url` عند توفره إطلاقاً رغم أنه أدق مصدر ممكن عند وجوده (كما ثبت مع id 66) — يستحق تحسيناً مستقبلياً: تفضيل حلّ `map_url` أولاً قبل البحث بالاسم/الإحداثيات المقرَّبة | لا شيء في كود المستودع (تحديث يدوي مباشر لصفَّي `stores` id 17 و66 عبر Supabase MCP فقط)، `CLAUDE.md` | لا ينطبق (لم يُعدَّل `app.js`/`styles.css`/`index.html`/`sw.js`) |
| 2026-07-12 | تعبئة تقييمات Google الحقيقية لدليل دكانجي (/dalil) كانت معلّقة منذ جلسة بناء الميزة (نفس اليوم): dry-run نجح (65/74 تطابق) لكن الكتابة الفعلية حُجبت مرتين من مصنّف الصلاحيات رغم موافقة عامة سابقة «قم بذلك» — الكتابة الجماعية على ~70 صف إنتاج حي تطلّبت تأكيداً محدداً بالاسم (موثّق في memory) | ليس خطأ برمجياً — بوابة موافقة متعمّدة فقط. المستخدم أرسل التأكيد المحدَّد بالضبط بالصيغة الموثَّقة مسبقاً في memory كمثال مطلوب: «شغّل تحديث تقييمات الـ70 متجراً باستثناء 17 و22 و36 و66» (الاستثناءات الأربعة = مطابقات Google مشبوهة اكتُشفت في dry-run: id 17 أفغان كباب⇐NAVOI، id 22 قاضي ماركت⇐Çağrı Market، id 36 زيتونة باشاك شهير⇐تصادم مع id 42، id 66 الكنافة الفلسطينية⇐حلويات سنابل) | نُفِّذ `node scripts/update-google-ratings.js --exclude=17,22,36,66` فعلياً (لا dry-run) على 74 متجراً معتمداً ناقص الأربعة المستثناة = 70. النتيجة: **61 تحديث ناجح** (HTTP OK من Supabase PATCH لكل صف) و**9 تخطٍّ آمن** (لا تطابق موثوق ضمن نصف قطر 450م فتُركت الحقول فارغة كما هو مصمَّم — بلا كتابة خاطئة: ids 12 حلويات زيتونة-بهشة ايفلر، 13 حلويات عز الدين، 15 نور الشام، 19 قاضي ماركت باشاك شهير، 24 أبو الزلف، 50 صفا الشام، 64 فلافل صاج، 84 ملحمة الدوماني، 85 قرقوشة فرفوشة) و**صفر فشل**. تحقّق حي عبر preview على `/dalil`: `stores` global في المتصفح يحمل `googleRating/googleReviewsCount/googlePlaceId/googleMapsUrl/googleRatingUpdatedAt` فعلياً وطازجة (`2026-07-12T15:22:41Z`)، وصفحة الدليل تعرض "74 نتيجة في الدليل · 61 متجراً بتقييم Google موثّق" مع ترتيب "الأعلى تقييماً" صحيح (باشا بيتزيريا 4.9، ريحان للمكسرات 4.9، أجبان بيازيد 4.9...). **بيانات فقط عبر السكربت الموجود مسبقاً — لا تعديل كود** | لا شيء في كود المستودع (Supabase فقط: أعمدة `stores.google_*` عبر السكربت الموجود)، `CLAUDE.md` | لا ينطبق (لم يُعدَّل `app.js`/`styles.css`/`index.html`/`sw.js`) |
| 2026-07-12 | المستخدم أرفق حزمة Next.js جاهزة (`ask-dukkanci-feature.zip`) وطلب إنشاء قسم «اسأل دكانجي» — مساعد يبني اقتراح طلب كامل من عدد الأشخاص/الميزانية/الوصف الحر. **اكتشاف حرج أثناء البدء:** المستودع كان في حالة `git merge dalil-guide` **عالقة منذ جلسة سابقة** (`MERGE_HEAD` موجود) بثلاثة ملفات تحوي علامات تعارض Git حرفية (`<<<<<<<`/`=======`/`>>>>>>>`) غير محلولة: `CLAUDE.md`، `styles.css`، `sw.js` — الأخيران لم يظهرا كـ"معدَّلين" في `git status` المختصر أول القراءة لأن الإخراج كان مقصوصاً (2K حرف)؛ التفصيل الكامل كشف `UU CLAUDE.md` و«You have unmerged paths». `styles.css` تحديداً كان يحتوي الكتلة الحية لتصميم `/category/<slug>` على الموبايل (من جلسة سابقة اليوم) **مقطوعة منتصف قاعدة CSS** (بلا قوس إغلاق) داخل علامات التعارض، تليها كتلة `/dalil` الكاملة (527 سطراً) خلف `>>>>>>> dalil-guide` — أي نشر بهذه الحالة كان سيُسقط جزءاً من كلا الميزتين بصمت (متصفحات CSS تتجاهل الأسطر المعطوبة) | تعارض الدمج نشأ لأن فرعي `main` و`dalil-guide` أضافا كتلتي CSS مختلفتين تماماً في نفس نقطة نهاية الملف (لا تعارض منطقي حقيقي، فقط تعارض نصي). لم يُكتشف سابقاً لأن جلسة `dalil-guide` وثّقت هذا بالضبط في سجلها (سطر 2026-07-12 السابق أدناه): إصلاح قوس `@media` مفقود لـfbads كان قد حُلّ تلقائياً بلا تعارض (لا تداخل مع main)، لكن كتلتي الإلحاق في ذيل الملف تداخلتا فعلاً ولم يُكمل أحد `git commit` لإنهاء الدمج | **حلّ التعارض (بلا حذف — دمج كِلا الجانبين):** استخرجت النسخة الكاملة غير المقطوعة من كل جانب مباشرة عبر `git show HEAD:styles.css` (نهاية main، ٧٥ سطراً) و`git show MERGE_HEAD:styles.css` (كتلة `/dalil` الكاملة، ٥٢٧ سطراً) وألصقتهما خلف الجزء المشترك غير المتعارض، فحصت توازن الأقواس برمجياً (عمق نهائي = صفر) قبل الاعتماد. `CLAUDE.md`: أُبقي سطرا سجل الإصلاحات كليهما (لا حذف تاريخ أبداً) بترتيب HEAD ثم dalil-guide. `sw.js` كان محلولاً فعلاً في مجلد العمل (غير مُضاف فقط) فتم قبوله كما هو. نُفِّذ `git add` للثلاثة (**لم يُنفَّذ `git commit` — الدمج لا يزال "قيد التنفيذ" عمداً**، بانتظار مراجعة المستخدم لأن الدمج يحمل تغييرات غير متعلقة بمهمة هذه الجلسة: `/dalil`, ترحيل تقييمات Google، إلخ). **الميزة:** بُنيت بالكامل داخل بنية SPA الحالية (وليس Next.js من الحزمة المرفقة — المشروع الفعلي vanilla JS): مسار `/ask-dukkanci` (`renderAskDukkanciPage`)، ومحرك اقتراحات **من طرف العميل بالكامل** (`askDukkanciBuildOptions` وما يتفرّع عنه) يعمل مباشرة على `products`/`stores` المحمَّلة أصلاً في المتصفح (`applyPublishingRules`, `isStoreApproved`, `isStoreOpenNow`, `store.subscriptionActive`) — بلا اختراع منتج/سعر/متجر إطلاقاً. المطابقة النصية تعيد استخدام `getMatchingProducts` الموجودة (فهرس مرادفات اللهجات) مع طبقة ترجيح جديدة (تكرار الكلمة عبر الكتالوج كمقياس تخصيص: كلمة نادرة كـ"مشاوي" تتفوّق على كلمة عامة كـ"مقبلات" التي تطابق عناصر عشوائية كموزة/خبز) بعد اكتشاف أن التطابق الخام كان يُرجّح أرخص عنصر بصرف النظر عن الصلة. الكمية تقديرية من `product.unit` (وزن للكيلو، تقسيم أشخاص/2 أو /3 للقطعة) موصوفة صراحة كتقدير قابل للتعديل. كل باقة **من متجر واحد إلزامياً** (لا تحايل على قيد السلة أحادي المتجر — بل تصميم يتجنّبه من الأساس)، ورسوم التوصيل حقيقية عبر `activeDeliveryQuote` عند توفر عنوان العميل، وإلا تصنيف صريح "(تقديري)". زر «أضف الباقة» يتحقق أولاً من `isProfileComplete()` قبل الحلقة (نافذة «أكمل بياناتك» الموجودة تدعم منتجاً واحداً معلّقاً فقط، فاستدعاء `addToCart` في حلقة كان سيفتحها عدة مرات ويدّعي نجاحاً كاذباً لسلة فارغة — أُصلح ليعرض تنبيهاً صادقاً بدل ذلك). تتبّع Analytics عبر `window.DUKKANCI_TRACKING.track()` الموجود بالأسماء الخمسة المطلوبة تماماً (`ask_dukkanci_opened/submitted/result_viewed/bundle_added/checkout_completed` — الأخير عبر علم جلسة `askDukkanciCartOrigin` يُقرأ عند نجاح `commitOrder` بجانب حدث `Purchase` الموجود). رابط في `desktop-nav` والفوتر (لم يُضَف لِـ`mobile-nav` — نفس نمط `/dalil` الذي اتُّبع بالفعل، الشريط السفلي محجوز عمداً عند 7 عناصر)؛ بانر CTA مدمج في الصفحة الرئيسية بعد "متاجر قريبة منك" (مرئي على الموبايل خلافاً لأقسام التسويق الأخرى المخفية هناك، لأنه مسار شراء مباشر لا نص تسويقي). **إصلاح أداء حرج أثناء الاختبار:** أول تنفيذ لـ`askDukkanciFindAddon` (البحث عن حلويات/مشروبات إضافية) كان يستدعي `getMatchingProducts` (تفحص الكتالوج الكامل ~8500 منتج) لكل كلمة مفتاح × لكل متجر مؤهّل — بلا وصف نصي وميزانية كبيرة (كل المتاجر الـ75 مؤهّلة) هذا يعني ~75×14 فحصاً كاملاً للكتالوج فتجمّد المتصفح فعلياً (>30 ثانية)؛ أُصلح بتحديد البحث لمنتجات المتجر نفسه فقط (أُصبح <500ms حتى لأقصى حالة: 100 شخص، بلا وصف، حلويات+مشروبات). تحقّق شامل عبر preview محلي: بحث "مشاوي" أعاد أطباق مشاوي حقيقية من ملحمة الهلال/نور الشام (وليس موزة/خبز عشوائيَين كما في أول تنفيذ)، تدفق كامل حي (تعبئة → إرسال → 3 بطاقات نتائج حقيقية → إضافة للسلة الحقيقية مع أسعار/كمية/توصيل صحيحة رياضياً → فتح درج السلة)، بوابة إكمال البيانات تعمل بصدق بدل الادّعاء الكاذب، صفر أخطاء كونسول على الصفحة والرئيسية و`/stores` و`/dalil` و`/category/restaurants` (تأكيد عدم كسر شيء من حل تعارض `styles.css`)، صفر انزياح أفقي على 375px، `npm run check:versions` ناجح. **لم يُختبر آلياً:** `ask_dukkanci_checkout_completed` (يتطلب تجاوز بوابة OTP واتساب الحية) — تحقّق بمراجعة الكود فقط أنه بجانب استدعاء `Purchase` الموجود مباشرة | `app.js`, `styles.css`, `index.html`, `sw.js`, `merchant-app.html`, `customer-app.html`, `CLAUDE.md` (ميزة اسأل دكانجي) + `styles.css`, `sw.js`, `CLAUDE.md` (حلّ تعارض دمج `dalil-guide`، **لم يُنفَّذ `git commit` بعد — راجع الأمر قبل إكمال الدمج**) | نعم — `npm run check:versions` ناجح (app.js v277، styles.css v169، CACHE v197)؛ **دمج `dalil-guide` لا يزال قيد التنفيذ محلياً، لم يُدفع، ولم يُكتمل بـcommit عمداً** |
| 2026-07-12 | المستخدم طلب إنشاء حملة إعلانات فيسبوك لمتجر قرقوشة فرفوشة (85) بنفس طريقة حملة باشا بيتزيريا السابقة (نطاق 2 ميل حول موقع المتجر، 50 ليرة/يوم، خصم 10% بمناسبة الافتتاح)، ثم طلب نشرها فعلياً، ثم طلب التأكد أن بكسل فيسبوك (Meta Pixel) مرتبط بكل الحسابات الإعلانية وربطه إن لم يكن كذلك | (1) لا توجد أداة مخصّصة لإنشاء ad set منفصلة في أدوات Marketing API المتاحة — تطلّب تمرير `adset_spec` كـJSON داخل استدعاء `ads_create_ad` مع `ad_set_id` وهمي. (2) **اكتشاف جوهري:** البكسل الحقيقي النشط فعلاً على الموقع ("Dukkanci Website Pixel"، `3926149691027789`، يستقبل أحداثاً حية) يتبع Business Manager مختلف تماماً ("Dukkanci Marketplace -New"، `1356340829738702`) عن الحسابين الإعلانيين المستخدَمين للحملات — تحقّق عبر API (`ads_get_datasets` لكل حساب) **و**يدوياً داخل Meta Business Suite (تبويب Connected assets: "You don't have any connected assets", 0 partners). محاولة مشاركته (Assign partner) مع الـBusiness الآخر رفضها فيسبوك نفسه: **"Businesses new to Facebook Products can share this asset... after several weeks"** — هذا الـBusiness أُنشئ 2026-06-26 (~أسبوعين فقط). كبديل، محاولة توليد Conversions API access token جديد للبكسل الاحتياطي المرئي فعلاً للحساب الإعلاني ("Dukkanci Pro - Pixel"، `2102305900374021`) اصطدمت بقيد صلاحية **منفصل**: "You must be an admin or developer for this business portfolio to create an access token" (حساب Chrome المسجَّل دخوله ليس admin/developer على الـBusiness المالك لتطبيق CAPI `1417611033524816`) | **الحملة:** أُنشئت حملة → ad set → إعلان كاملة عبر Marketing API على الحساب `938042394454448` (نفس حساب باشا بيتزيريا): `قرقوشة فرفوشة - AR 18-55` (OUTCOME_TRAFFIC، ميزانية على مستوى ad set/ABO)، استهداف `custom_locations` نطاق 2 ميل حول إحداثيات المتجر الفعلية في Supabase (41.015167, 28.686551 — تطابق شبه تام مع Plus Code الذي أرسله المستخدم)، الأعمار 18-55، الإعلان يعيد استخدام غلاف المتجر المستضاف فعلاً كصورة + نص "خصم 10% بمناسبة افتتاح المتجر على دكانجي" + زر ORDER_NOW → رابط `/store/karkousha-farfousha-sweets`. كل الكيانات أُنشئت PAUSED افتراضياً (لا إنفاق) ثم فُعِّلت من الأعلى للأسفل (حملة←ad set←إعلان) فقط بعد طلب المستخدم الصريح «انشر الاعلان»؛ تحقّق حي عبر `ads_get_ad_entities` أظهر توصيلاً فعلياً خلال ساعات (442 ظهور، 8 نقرات رابط، إنفاق 16.55 ليرة). **البكسل (حل مؤقت بموافقة المستخدم الصريحة «بدّل البكسل إلى Dukkanci Pro مؤقتاً»):** بدّلت `integration_settings.meta_pixel_id` من البكسل المحظور إلى `2102305900374021` (البكسل المرئي فعلاً لحساب الحملات) — يسري فوراً على المتصفح لأن `integrations.js` يقرأ هذا الجدول مباشرة عبر Supabase client بلا حاجة نشر كود. وعطّلت `meta_capi_token` (قيمة فارغة + `is_enabled=false`) بدل تركه يفشل بصمت بمفتاح لا يطابق البكسل الجديد — تتبّع الخادم (Conversions API) متوقّف مؤقتاً، تتبّع المتصفح فقط يعمل حالياً. البكسل الأصلي وتاريخ أحداثه المتراكم لم يُمسّا، وسيُعاد التبديل إليه بمجرد انتهاء فترة حظر المشاركة أو الحصول على صلاحية admin على الـBusiness الآخر | لا شيء في كود المستودع (كائنات Meta Ads عبر API + صف `integration_settings` في Supabase فقط)، `CLAUDE.md` | لا ينطبق (لم يُعدَّل `app.js`/`styles.css`/`index.html`/`sw.js`). **مهمة مفتوحة لجلسة قادمة:** إعادة `meta_pixel_id` للبكسل الأصلي + إعادة تفعيل `meta_capi_token` بعد حل قيد المشاركة، وتوحيد البكسلات الثلاثة المتناثرة (Dukkanci Website Pixel / Dukkanci pixels / Dukkanci Pro - Pixel) في بكسل واحد مربوط بشكل صحيح |
| 2026-07-12 | المستخدم طلب فحص باقي المتاجر بحثاً عن صور ضخمة كغلاف قرقوشة فرفوشة (2.4MB) الذي اكتُشف وأُصلح في نفس اليوم | مسح شامل (HEAD لكل رابط) لـ122 صورة فريدة من حقول `stores` الثلاثة (75 متجراً) + كل الصور المتداخلة في `site_settings` كشف **11 ملفاً >300KB داخل `assets/photos/` بالمستودع** بمجموع 6.4MB، أسوأها: غلاف ملحمة الهلال (id 5) **1969KB** بأبعاد 2400×1600 PNG فوتوغرافي، شعار مطعم رودي (id 51) 732KB بأبعاد 1207×1029 لعرض 66px، غلاف الوادي 639KB (JPEG 800×637 غير مضغوط إطلاقاً!)، وصورة بلاطة قسم الخضار على الرئيسية (`product-vegetable-basket.jpg`، من `site_settings.categories`) 608KB — **6 من الـ11 مُدرَجة في `APP_SHELL` بـ`sw.js` فتُنزَّل مسبقاً على جهاز كل زائر يثبّت الـSW** | تصغير الملفات الـ11 **في مكانها** (نفس المسار والامتداد فلا يتغيّر أي مرجع في DB أو كود أو `sw.js`) عبر `sharp`: الأغلفة ≤1280px بـmozjpeg q78 (وPNG الهلال بـpalette PNG q90)، الشعارات ≤400px palette PNG. النتيجة 6.4MB→1.6MB (توفير 4.8MB). تحقّق بصري فعلي (فتح الصور المصغّرة) أكّد عدم وجود تشوه. **قرار متعمد: لم يُرفَع `CACHE` في `sw.js`** لأن جلسة متزامنة أخرى كانت تحمل تعديلات غير محفوظة على `sw.js` نفسه لحظة العمل (خطر تداخل موثّق) — الزوار الجدد وغير مثبّتي الـSW يستفيدون فوراً، ومثبّتو الـSW الحاليون سيلتقطون النسخ الخفيفة تلقائياً مع أول رفع `CACHE` قادم من أي مهمة | 11 ملف صورة تحت `assets/photos/`, `CLAUDE.md` | لا ينطبق (لم يُعدَّل أي كود؛ رفع CACHE مؤجَّل عمداً لأول نشر قادم — انظر السبب) |
| 2026-07-12 | بعد إصلاح متجر "ملحمة الدوماني" (id 84)، طلب المستخدم أولاً التأكد أن الروابط القديمة لا تزال تعمل، ثم طلب تنظيف اسم المتجر القديم في `store-slugs.js` | `store-slugs.js` كان يحمل `84: "zamzam-market"` — اسم قديم خاطئ أُضيف في 2026-07-08 رغم أن المتجر id 84 كان بالفعل "ملحمة الدوماني" منذ إنشائه في 2026-07-05 (تحقّق من Supabase: لا وجود لأي متجر باسم "زمزم"/"Zamzam" على المنصة حالياً)، على الأرجح خطأ نسخ عند إضافة slugs للمتاجر 84/85 معاً في نفس الالتزام. `getStore()` في `app.js` يتحقق من `STORE_SLUG_TO_ID` (هذا الملف) أولاً، فكان `/store/zamzam-market` يعمل بالصدفة رغم عدم توافقه مع `store.slug` الفعلي في Supabase ("domani") | تحقّق أولاً أن كل الروابط الشرعية للمتجر (`/store/84` الرقمي، `/store/domani` الحالي، وحتى `/store/zamzam-market` القديم) تعمل قبل أي تعديل. ثم بعد طلب التنظيف الصريح: عدّل `store-slugs.js` من `84: "zamzam-market"` إلى `84: "domani"` ليطابق `stores.slug` في Supabase (تأكدت أولاً عبر بحث Supabase أن لا متجر حقيقي آخر يستخدم اسم "زمزم" فيحتاج هذا الـslug). النتيجة: `/store/84` و`/store/domani` يعملان كما هما (لم يتغيّر شيء فعلياً بالنسبة لهما لأن `store.slug` في Supabase كان يغطيهما أصلاً)، بينما `/store/zamzam-market` أصبح 404 عمداً — تحقّقت أنه لا يوجد أي رابط عام معروف/مُشارك يعتمد عليه (المتجر جديد، لا تقييمات، لا تاريخ تسويقي بهذا الاسم). تحقّق كامل عبر preview محلي (`localhost:4173`) للحالات الثلاث قبل الدفع. رُفع الإصدار: `store-slugs.js` v234→v235 (في `index.html` ومصفوفة `APP_SHELL` بـ`sw.js`)، و`CACHE` v190→v191 في `sw.js` | `store-slugs.js`, `index.html`, `sw.js`, `CLAUDE.md` | نعم (يدوياً بالبحث النصي في الملفين؛ `check:versions` لا يزال غير مربوط بـpackage.json على main) |
| 2026-07-12 | المستخدم أبلغ (بلقطة شاشة من موبايله لصفحة `/category/restaurants`) أن صفحة القسم على الموبايل مليئة بالفراغات والمساحات الفارغة وغير جذابة، وطلب إعادة تصميمها بشكل لائق | ثلاثة مصادر للفراغ على الموبايل تحديداً: (1) هيرو الصفحة (`.page-hero.compact`) ورث حواشي سطح المكتب الضخمة (42px فوق/تحت + عنوان 34px) فيصنع شاشة شبه فارغة قبل أي محتوى؛ (2) بانر "أعلن عن متجرك" (المُضاف في جلسة 2026-07-11 لملء فراغ حقيقي كان بين الهيدر والشبكة) كان على الموبايل بطاقة عمودية مكدّسة بالوسط (دائرة أيقونة 130px + عناوين كبيرة + زر) تشغل كامل الشاشة الأولى تقريباً قبل ظهور أي متجر قابل للنقر — عكس فلسفة "الشراء أولاً" المعتمدة للموبايل؛ (3) عنوان القسم كان يتكرر مرتين متتاليتين (h1 الهيرو ثم عنوان الشبكة مباشرة) | إعادة تصميم موبايل فقط (≤640px عبر media query جديد آخر `styles.css`، سطح المكتب دون أي تغيير): تصغير هيرو القسم (فئة جديدة `category-hero` كي لا تتأثر صفحات `page-hero` الأخرى؛ حواشٍ 16/12px وعنوان 26px)، إخفاء عنوان الشبكة المكرر، نقل البانر ليأتي بعد شبكة المتاجر وقبل "منتجات مختارة" عبر flex order على حاوية القسم (ترتيب DOM بقي كما هو حفاظاً على تخطيط سطح المكتب المعتمد — لُفّت الشبكة والمنتجات في `.category-stores-block`/`.category-products-block` داخل `renderCategoryPage`)، وتحويل البانر لشريط أفقي مدمج (أيقونة 56px + نص يمين + زر بعرض كامل؛ ارتفاعه أصبح ~171px بدل شاشة كاملة). تحقّق عبر preview محلي: موبايل 375px — الشاشة الأولى تعرض العنوان+العدّاد وبطاقات المتاجر مباشرة (الشبكة تبدأ عند 332px من أعلى المستند)، البانر المدمج يظهر بعد المتاجر بالشكل الجديد، صفر أخطاء كونسول وصفر انزياح أفقي (scrollWidth=375)، واختُبر قسمان فعلياً (`restaurants` بـ35 متجراً و`butcher` بمتجرين)؛ سطح المكتب بقي بالتخطيط الأصلي تماماً (بانر أعلى الصفحة بأعمدته الثلاثة + عنوان الشبكة ظاهر + هيرو 54/58px). ملاحظة بيئة: التقاط لقطات الشاشة في Browser pane كان يفشل بمهلة متكررة أول الجلسة (مشكلة معروفة من جلسات سابقة) فاعتُمد التحقق بقياسات getComputedStyle/getBoundingClientRect ثم نجحت اللقطات لاحقاً وأكّدت النتيجة بصرياً. رُفع الإصدار: `app.js` v271→v272 و`styles.css` v167→v168 (في المواضع الأربعة) و`CACHE` v189→v190 في `sw.js` | `app.js`, `styles.css`, `index.html`, `sw.js`, `merchant-app.html`, `customer-app.html`, `CLAUDE.md` | نعم (يدوياً بالبحث النصي؛ `check:versions` غير مربوط بـpackage.json على main) |
| 2026-07-12 | (تكملة لسطرَي اليوم أدناه، بموافقة المستخدم «نفّذ إصلاح جذر المشكلة») إغلاق جذر فئة أخطاء base64-في-القاعدة نهائياً: كل رفع صورة من التاجر/الإدارة/نموذج الانضمام كان يُخزَّن كنص base64 خام داخل صفوف `products`/`stores`/`site_settings` فيعيد إنتاج مشكلة تضخّم/تجمّد الصفحات مع كل تاجر جديد | `readImageFileResized()` في `app.js` (6 مواضع استدعاء: شعار نموذج الانضمام ×2، صورة المنتج، غلاف المتجر، صور الأقسام والمحتوى بالإدارة) تحوّل الصورة لـ`data:` URL يُرسَل كما هو داخل JSON الحفظ، وكل مسارات الكتابة الثلاثة في `api/notify-order.js` (`save-product`, `save-store`, `save-settings`) كانت تكتب القيمة للقاعدة بلا أي تحويل | **اعتراض من طرف الخادم** (قرار تصميمي: نقطة واحدة تغطي كل المسارات الحالية والمستقبلية بلا أي تعديل على `app.js` وبلا endpoint جديد مفتوح — المصادقة الموجودة أصلاً في المعالجات تبقى هي البوابة): أُضيفت لـ`api/notify-order.js` دوال `offloadInlineImage()` (فك base64 → `sharp` resize ≤1280px WebP q80 — lazy require فلا تكلفة إقلاع إلا عند وصول صورة فعلاً — → رفع لباكت `campaign-images` بمفتاح service role بنمط `api/campaign.js` → إرجاع الرابط) و`offloadRowImages()` (لحقول صف مسطّحة) و`offloadJsonImages()` (مشي تكراري لأي عمق داخل JSON الإعدادات، بسقف 20 صورة/طلب ضد الإساءة)، واستُدعيت قبل `sbWrite` في المعالجات الثلاثة: `save-product` (حقل `image`)، `save-store` (`image`, `cover_image`, `logo_image` — يغطي نموذج الانضمام لأنه يمر بنفس المسار عبر x-sb-token)، `save-settings` (كامل `value` تكرارياً — الجدول يُجلب في كل فتح صفحة فأهميته قصوى). **سياسة الفشل fail-open:** أي خطأ رفع → تبقى القيمة الأصلية (السلوك القديم) مع تحذير سجلّ، فلا يُحجب حفظ التاجر أبداً بسبب عطل تخزين. `sharp` تبعية إنتاج مثبتة تعمل فعلاً على Vercel (`api/catalog-process-image.js` يستخدمها). اختُبر كاملاً على الخادم المحلي (`server.js` :4173، admin token حقيقي، بيانات تجريبية أُنشئت ثم حُذفت مع ملفاتها المرفوعة): منتج تجريبي بصورة base64 → حُفظ برابط `store84_p9990001_image_*.webp` يُرجع HTTP 200 فعلياً؛ إعدادات بصور متداخلة (كائن + داخل مصفوفة) → كل الصور تحوّلت لروابط؛ `save-store` أثبت التحويل أيضاً (ظهر الرابط المحوَّل في رسالة خطأ NOT NULL لأن الاختبار أرسل صفاً ناقصاً عمداً — لا يحدث واقعياً لأن `toDbStore` يرسل الصف كاملاً، وupsert فشل ذرّياً فلم يمسّ متجر 84) | `api/notify-order.js`, `CLAUDE.md` | لا ينطبق (خادم فقط — لم يُعدَّل `app.js`/`styles.css`/`index.html`/`sw.js`، لا حاجة لأي رقم إصدار). **مدموج في `main` محلياً، غير مدفوع بعد — يحتاج push ليصل للموقع الحي** |
| 2026-07-12 | (تكملة للسطر التالي أدناه بموافقة المستخدم الصريحة «نفّذ ترحيل صور base64 المتبقية») ترحيل آخر 37 صورة منتج base64 + شعار متجر 85 التي كانت تتضخّم بها استجابات جلب كل المنتجات في كل صفحات الموقع | نفس فئة الجلسة السابقة: صور رُفعت عبر لوحة التاجر/الانضمام الذاتي فخُزّنت كنص base64 خام داخل صفوف `products.image` و`stores.logo_image` — 20 صورة لصفا الشام (50)، 6 لباشا بيتزريا (56)، 11+شعار لقرقوشة فرفوشة (85)، بمجموع ~2.6MB. **اكتشاف إضافي أثناء التحقق:** غلاف متجر 85 (`cover_image`) كان مستضافاً كرابط لكن الملف نفسه 2.4MB بأبعاد 2172×724 (صورة ChatGPT رُفعت كما هي بلا ضغط) — نفس فئة مشكلة شعار الدوماني | سكربت `migrate-base64.js` (scratchpad الجلسة): جلب كل صف `image LIKE 'data:%'` بمفتاح service role، تصغير بـ`sharp` إلى ≤800px WebP جودة 80 (الشعارات 320px)، رفع لباكت `campaign-images` بأسماء `store{id}_p{pid}_{ts}.webp`، ثم PATCH للصف بالرابط القصير — 38/38 نجحت بلا أخطاء، والأحجام انخفضت ~50-60% إضافية عن base64 الأصلي بفضل WebP. غلاف متجر 85 صُغّر أيضاً إلى 1280px WebP = 100KB (من 2399KB) وحُدّث `cover_image`. تحقّق نهائي: استعلام SQL يؤكد **صفر** صفوف base64 متبقية في `products` و`stores` على مستوى المنصة كلها؛ الصفحات الحية للمتاجر الثلاثة (`karkousha-farfousha-sweets`, `safa-alsham-market`, `pasa-pizzeria-restaurant`) تعرض عناوينها وكل صورها، عينات الملفات الجديدة تُرجع HTTP 200، وصفر صور base64 في DOM أي صفحة | لا شيء في كود المستودع (Supabase فقط: Storage + `products.image` + `stores.logo_image`/`cover_image`)، `CLAUDE.md` | لا ينطبق (لم يُعدَّل أي ملف واجهة) |
| 2026-07-12 | المستخدم أبلغ أن صفحة `/store/domani` لا تزال "تتجمد قليلاً عند الفتح" رغم إصلاح base64 في جلسة 2026-07-11، وطلب فحص كل الأخطاء وإصلاحها | قياس فعلي على الموقع الحي (Resource Timing + Long Tasks): **شعار المتجر الذي رُفع في جلسة 2026-07-11 نفسها (`domani_logo_1783781744838.png`) كان 1.28MB بأبعاد 1254×1254 بينما يُعرض 66×66 فقط** — أكبر ملف في الصفحة كلها (أكبر من كل صور المنتجات الـ11 + الغلاف مجتمعة)، تنزيله ~2 ثانية وفكّ ترميز 1.57 مليون بكسل على الخيط الرئيسي يجمّد الموبايل. صور المنتجات والغلاف كانت سليمة (~100-150KB). **اكتشاف جانبي أثناء الفحص:** لا تزال 37 صورة منتج base64 (~2.6MB) في 3 متاجر أخرى (85 قرقوشة فرفوشة: 11 صورة + شعار المتجر نفسه، 50 صفا الشام: 20، 56 باشا بيتزريا: 6) تتضخّم بها استجابات جلب كل المنتجات المقسّمة صفحات (لوحظت صفحة offset=5000 تستغرق 24 ثانية) — تؤثر على التحميل الخلفي في كل الصفحات | تصغير الشعار بـ`sharp` (متوفرة في node_modules) إلى 320×320 WebP جودة 82 = **11KB (تخفيض 99.2%)**، رفعه لباكت `campaign-images` باسم `domani_logo_optimized_320.webp` بمفتاح service role من `.env` المحلي، وتحديث `stores.logo_image` للمتجر 84 عبر Supabase MCP. تحقّق حي: الصفحة تحمّل الشعار الجديد (HEAD يؤكد 11KB image/webp HTTP 200، naturalWidth 320)، 11 منتجاً ظاهرة، صفر أخطاء كونسول. **لم تُنفَّذ** ترحلة الـ37 صورة base64 للمتاجر 50/56/85 — مصنّف الصلاحيات رفضها كتوسيع نطاق خارج طلب المستخدم (سكربت جاهز في scratchpad الجلسة `migrate-base64.js`)، تحتاج موافقة صريحة من المستخدم في جلسة قادمة. **درس مستفاد:** أي رفع صورة مستقبلي لـ Supabase Storage يجب أن يمرّ بتصغير/ضغط أولاً (sharp resize ≤800px + webp) — رفع الأصل كما هو أعاد إنتاج نفس فئة مشكلة التجمد التي كان يُفترض أنه يحلّها. الجذر الأعمق لم يُعالَج بعد: `readImageFileResized` في نموذج انضمام التاجر لا يزال يخزّن base64 خاماً في الصفوف (مصدر صور متجر 85) | لا شيء في كود المستودع (Supabase فقط: Storage + `stores.logo_image`)، `CLAUDE.md` | لا ينطبق (لم يُعدَّل `app.js`/`styles.css`/`index.html`/`sw.js`) |
| 2026-07-12 | المستخدم طلب قسماً جديداً «دليل دكانجي» على `/dalil`: دليل متاجر بتقييمات Google الرسمية (بلا scraping) مع بحث بالاسم/المنتج/المنطقة، فلترة بالمنطقة والتصنيف و«مفتوح الآن» و«التوصيل متاح»، ترتيب بالأعلى تقييماً/الأكثر تقييمات/الأقرب، مقارنة حتى 3 متاجر على `/dalil/compare?ids=..`، بطاقة تعرض التقييم وعدد التقييمات وتاريخ التحديث ورابطَي المتجر والاتجاهات، حد أدنى 50 تقييماً (قابل للتغيير من الإعدادات) لتصدّر «الأعلى تقييماً»، وسم «مميز» بلا خلط بالترتيب العضوي، وSEO كامل | ميزة جديدة (لا يوجد سبب جذري)، لكن اكتُشف وأُصلح **خللان قائمان مسبقاً** أثناء البناء: (1) 🔴 كتلة `@media (max-width: 640px)` لأنماط fbads (سطر ~11463 في `styles.css`) **بلا قوس إغلاق منذ إنشائها**، فكانت تحبس كل ما بعدها في الملف (~490 سطراً: كل أنماط "Homepage conversion additions" — trust-bar، search-chips، product rails، whatsapp-assist...) داخل شرط ≤640px بصمت — بدا الموبايل سليماً لأن معظمها أنماط موبايل أصلاً، بينما كانت ميتة تماماً على سطح المكتب في الموقع الحي (شرائح البحث مثلاً كانت بلا تصميمها المقصود على الشاشات الواسعة رغم "إصلاح" لونها في 2026-07-11 الذي جرى التحقق منه على 375px فقط). أي CSS جديد يُلحق بنهاية الملف كان سيُبتلع في نفس الفخ — وهذا ما حدث فعلاً لأنماط الدليل قبل اكتشافه. (2) `scripts/check-versions.js` الموثّق في قسم 4 أعلاه **لم يكن متتبَّعاً في git إطلاقاً** (موجوداً في مجلد العمل فقط) — لهذا كان `npm run check:versions` "غير مربوط" في كل الجلسات السابقة | **(أ) الميزة:** أعمدة جديدة آمنة في `stores` عبر migration (`google_place_id/google_rating/google_reviews_count/google_maps_url/google_rating_updated_at` — طُبّقت على Supabase باسم `store_google_ratings_dalil`) + صف `site_settings.dalil` بقيمة `{"minGoogleReviewsForTop":50}` يقرؤه العميل مع احتياطي 50. ملف مشترك جديد `dalil-regions.js` (13 منطقة بكلمات مطابقة عربية/تركية، اشتقاق منطقة المتجر من لاحقة اسمه ثم عنوانه، نفس نمط `category-slugs.js`: `<script>` للواجهة + `require` لـSSR/sitemap). في `app.js`: راوت `dalil` + `renderDalilPage`/`renderDalilComparePage` + فلترة/ترتيب عضوي بحت (المؤهَّلون ≥الحد الأدنى يتقدمون دائماً في «الأعلى تقييماً»؛ `featured` شارة «مميز» فقط ولا يرفع الترتيب أبداً) + بحث يشمل المنتجات عبر `getMatchingProducts` الموجودة + مزامنة كاملة بين الفلاتر ورابط الصفحة (`?category=&region=&q=&open=&delivery=&sort=`) + `updateHead` يعطي عنواناً/وصفاً/canonical ديناميكياً لكل تركيبة تصنيف/منطقة وItemList JSON-LD حيّاً (يُزال خارج القسم) وnoindex لصفحة المقارنة. قشرة SSR جديدة `api/dalil.js` (نمط `api/store.js`: shell + استبدال meta + ItemList/Breadcrumb JSON-LD + روابط `/store/<slug>` داخل المصدر للزحف) مربوطة في `vercel.json` (`/dalil` و`/dalil/compare`) — محلياً تُختبر عبر `/api/dalil` مباشرة مع `SSR_SHELL_ORIGIN`. sitemap: `/dalil` + متغيرات التصنيف والمنطقة في `sitemap-pages`. سكربت `scripts/update-google-ratings.js` (Places API بنفس مفتاح `api/maps-key` الحي: Nearby+FindPlace حول إحداثيات المتجر مع **رفض أي تطابق أبعد من ~450م** — ترك الحقل فارغاً أفضل من تقييم متجر خاطئ؛ `--dry-run` و`--only=ids`؛ إعادة تشغيله لاحقاً تحدّث التقييمات عبر place_id المحفوظ مباشرة). **التعبئة الكاملة لم تُنفَّذ**: مصنّف الصلاحيات رفض تعديل 75 صفاً حياً دون إذن صريح — dry-run كامل نجح (65/74 تطابق، 9 تخطٍّ آمن، ومطابقات تحتاج مراجعة: id 22 قاضي ماركت⇐Çağrı Market مرفوضة في تدقيق 2026-07-10، id 17، 36، 66)، وزُرع متجران فقط (56 باشا بيتزريا 4.9/731، 58 برياني بالاس 4.4/3855 بقيم حقيقية من dry-run) للتحقق البصري. **(ب) الخللان:** أُغلق قوس fbads المفقود (مع تعليق شرح مكانه)، وأُدخل `scripts/check-versions.js` في git ورُبط أخيراً بـ`package.json` (`npm run check:versions` يعمل الآن فعلاً). تحقّق شامل عبر preview محلي: موبايل 375px (بطاقات وفلاتر وعربة مقارنة)، فرز فعلي بالحالتين، منع المتجر الرابع من المقارنة برسالة، إزالة متجر من المقارنة تحدّث ids والعودة للدليل تحت متجرين، «مفتوح الآن» أخفى كل المغلق، بانر «حدّد موقعي» عند «الأقرب» بلا موقع، رابط البطاقة ينقل لصفحة المتجر، سطح مكتب 885px (شبكة عمودين + شرائح البحث بألوانها المقصودة أخيراً)، `/stores` و`/category/restaurants` و`/offers` بلا أي تأثر، وSSR curl لكل المتغيرات (canonical/noindex صحيحة). ⚠ لقطات الشاشة معطّلة في بيئة الجلسة فكان التحقق نصياً/محسوباً (سابقة موثّقة في ذاكرة المشروع) | `app.js`, `styles.css`, `index.html`, `sw.js`, `vercel.json`, `package.json`, `api/dalil.js` (جديد), `dalil-regions.js` (جديد), `api/sitemap.js`, `scripts/update-google-ratings.js` (جديد), `scripts/check-versions.js` (أُدخل في git), `migrations/20260712_store_google_ratings.sql` (جديد), `merchant-app.html`, `customer-app.html`, `CLAUDE.md` | نعم — `npm run check:versions` ناجح ✅ (يعمل من package.json لأول مرة): app.js v271→272، styles.css v167→168 (في المواضع الأربعة)، CACHE v189→190، وأُضيف `dalil-regions.js?v=1` لـAPP_SHELL |
| 2026-07-11 | المستخدم أبلغ أن متجر "ملحمة الدوماني" (`/store/domani`) مكرَّر (يوجد متجر آخر بنفس الاسم) وأن صفحته "تتجمد قليلاً عند الفتح"، وطلب حذف المتجر المكرر ورفع شعار جديد (صورة "أبو ياسين" ذهبية/سوداء) لصفحة المتجر | (1) استعلام مباشر على Supabase أظهر متجرين بنفس الاسم "ملحمة الدوماني": id 84 (slug `domani`، أُنشئ 2026-07-05، 11 منتجاً) وid 86 (slug تلقائي `store-86`، أُنشئ في نفس يوم الطلب، صفر منتجات/طلبات) — كلاهما بنفس رقم هاتف التاجر في `store_credentials`، ما يؤكد أنه تسجيل مكرر لنفس التاجر وليس متجرين حقيقيين. (2) بالتدقيق في سبب التجمد: من أصل 75 متجراً معتمداً على المنصة، كان متجر id 84 هو **الوحيد** الذي يخزّن صورة الغلاف (`cover_image`) وصور الـ11 منتجاً كلها كنص base64 خام (~1.9MB إجمالياً) مباشرة داخل صفوف جدولي `stores`/`products`، بدل روابط خفيفة (~30 حرفاً) مستضافة كبقية كل متجر آخر على المنصة — هذا يحدث تلقائياً عندما يرفع التاجر صوره عبر نموذج الانضمام الذاتي (`readImageFileResized` في `app.js` يحوّل الصورة مباشرة إلى `data:` URL ويُخزَّن كما هو، بلا رفع فعلي لـ Supabase Storage)، فيضخّم حمل صفحة هذا المتجر تحديداً بشكل كبير جداً مقارنة بأي متجر آخر عند كل فتح | (1) حذف متجر id 86 بالكامل (الصف من `stores` + `store_credentials` + `store_users`) عبر Supabase MCP `execute_sql` بعد التأكد من خلوّه من منتجات/طلبات ومطابقة رقم هاتفه لـ id 84. (2) سكربت Node لمرة واحدة (محفوظ في scratchpad الجلسة، غير جزء من المستودع) يستخدم `SUPABASE_SERVICE_ROLE_KEY` من `.env` المحلي لتحويل كل base64 (الغلاف + 11 صورة منتج) إلى ملفات مرفوعة لباكت `campaign-images` في Supabase Storage (نفس النمط المستخدم فعلاً في `api/campaign.js`)، ثم تحديث الصفوف لتستخدم الروابط القصيرة الناتجة بدل base64 — أنزل ذلك حمل صفحة المتجر من ~1.9MB نص base64 إلى عشرات البايتات من الروابط، مطابقاً لبقية المتاجر. (3) رفع الشعار الجديد الذي أرفقه المستخدم في المحادثة — لم يكن متاحاً كملف عبر أي مسار مرفقات مباشر، فتم تحديده يدوياً كأحدث ملف صورة في مجلد التنزيلات المحلي (`ChatGPT Image Jul 11, 2026, 05_40_32 PM.png`) بمطابقة بصرية، ثم رفعه لنفس الباكت وتحديث `stores.logo_image`. تحقّق كامل عبر Browser pane على الموقع الحي: الصفحة تعرض 11 منتجاً بلا أخطاء JS، الروابط الجديدة (غلاف + شعار + منتجات) تُرجع HTTP 200 فعلياً، و`stores` أصبحت 75 صفاً بعد حذف المكرر | لا شيء في كود المستودع (Supabase فقط عبر REST/SQL مباشرة بمفتاح service role، بيانات حية تُطبَّق فوراً بلا نشر)، `CLAUDE.md` | لا ينطبق (لم يُعدَّل `app.js`/`styles.css`/`index.html`/`sw.js`، فقط بيانات Supabase) |
| 2026-07-11 | المستخدم أرفق نسخة من تطبيق منافس (ملف "bundle" ذاتي التشغيل) وطلب أن تصبح نسخة الموبايل من دكانجي "تطبيق بيع بدون كلام" — يوجّه الزائر نحو الشراء المباشر بدل استعراض فقرات تسويقية طويلة قبل الوصول للمتاجر والمنتجات؛ ثم بعد المعاينة طلب أن تُصمَّم الفئات كبلاطات كبيرة بصور (كالمرجع)، ثم لاحظ مساحة فارغة كبيرة عند فتح صفحة أي قسم (مثال: `/category/restaurants`) وطلب ملأها ببانر "أعلن عن متجرك هنا" مع استبدال عنوان "المتاجر" الثابت باسم القسم الفعلي — لكل الأقسام | (1) الصفحة الرئيسية على الموبايل كانت تكرّس ~9 أقسام متتالية قبل أي محتوى قابل للشراء فعلياً: فقرة الهيرو الترويجية المتحركة (4 شرائح)، صف بطاقات ثقة مكرّر، قسم "اطلب بثقة" (4 بطاقات، يكرّر نفس الرسائل)، "طلبك في ثلاث خطوات" (شرح بلا فعل شراء)، بانر مساعدة واتساب، وبانر توظيف تجار — كلها "كلام" لا يقود لشراء مباشر. (2) صفحة `/category/<slug>` كانت تترك فراغاً حقيقياً (~162px) بين الهيدر وقائمة المتاجر بسبب تراكم `padding` افتراضي (82px قسم + 38px هامش عنوان) بلا أي محتوى بينهما، والعنوان كان نصاً ثابتاً "المتاجر" بدل اسم القسم. (3) عند التدقيق تبيّن أن **3 من أصل 8 أقسام حيّة فعلياً** (مكسرات وبهارات، مواد غذائية متخصصة، مطابخ منزلية) لم يكن لها `slug` مخصّص إطلاقاً في `category-slugs.js` فكانت تنقل الزائر لصفحة "كل المتاجر" العامة بدل صفحة القسم — وأن قسم "ملاحم" كان يملك `slug` لكنه **معطوب بصمت منذ إنشائه**: كان يطابق النص الثابت "ملحمة ومشاوي" بينما القسم الفعلي في `HOME_CATEGORIES`/الموقع الحي اسمه "ملاحم"، فصفحته كانت تعرض صفر متاجر دائماً رغم وجود متاجر (ملحمة الهلال) تنتمي له فعلياً؛ فلتر `/stores` العام (`getFilteredStores`) كان يتعامل مع هذا بمطابقة خاصة (`store.category.includes("ملحم")`) لم تُستنسخ أبداً في صفحة القسم المخصّصة | **(1) الصفحة الرئيسية (موبايل ≤640px فقط، سطح المكتب دون أي تغيير):** أُخفيت الأقسام الستة المذكورة أعلاه بالكامل عبر media query جديد في `styles.css`، وأُضيفت مكانها بلاطات كبيرة بصورة خلفية (`shopTile()` في `app.js`، أول بلاطة كبيرة بعرض كامل ثم البقية بشبكة عمودين) تستخدم نفس بيانات/صور الأقسام الحقيقية الموجودة أصلاً (`homeCategoriesOrdered()`) — لا صور جديدة. أُخفي أيضاً شريط الأقسام الصغير القديم منتصف الصفحة على الموبايل تفادياً للتكرار (لا يزال يظهر على سطح المكتب). **(2) صفحة `/category/<slug>`:** استُبدل عنوان "المتاجر" الثابت بـ`${catText}` (اسم القسم الفعلي)، وأُضيفت بلاطة "لأصحاب المتاجر" (بنفس مكوّن `.merchant-cta` الموجود أصلاً بالصفحة الرئيسية، بلا CSS جديد) بنص ديناميكي "أعلن عن {اسم مفرد للقسم} هنا" عبر خريطة `CATEGORY_MERCHANT_PITCH` الجديدة (تغطي كل الأقسام السبعة القابلة للتخصيص + احتياطي عام "متجرك" لأي قسم مستقبلي غير مُدرَج) تملأ الفراغ بدل تركه فارغاً. **(3) تغطية الأقسام الثمانية الحية بالكامل:** أُضيفت 3 مداخل ناقصة في `category-slugs.js` (`nuts-spices`، `specialty-food`، `home-kitchens`)، وصُحِّح خلل "ملاحم" جذرياً باستخراج منطق المطابقة المشترك في دالة جديدة `storeMatchesCategory()` يستخدمها كل من `getFilteredStores()` و`renderCategoryPage()` معاً (بدل تكراره ناقصاً في مكان واحد فقط)، وصُحِّح مفتاح `category-slugs.js`/`CATEGORY_MERCHANT_PITCH` من "ملحمة ومشاوي" إلى "ملاحم" ليطابق القيمة الفعلية. **بيئة العمل:** كل التنفيذ تم في worktree + فرع منفصلين تماماً (`mobile-buy-first-experiment`) بناءً على طلب المستخدم الصريح عدم لمس أي شيء حي حتى الموافقة، ثم أُعيد ترتيبه (`git rebase`) فوق آخر حالة لـ`main` (كانت قد تقدّمت بـ3 التزامات من جلسة متزامنة أخرى أثناء العمل) قبل الدمج. تحقّق شامل عبر preview محلي (موبايل 375px): كل الأقسام الثمانية (بالنقر الفعلي من بلاطات الصفحة الرئيسية، وليس فقط بالرابط المباشر) أظهرت بانراً وعنواناً صحيحين ومتاجر فعلية ظاهرة (بما فيها "ملاحم" التي كانت صفراً دائماً سابقاً)؛ سطح المكتب (1280px) لم يتأثر بأي من تعديلات المرحلة (1) وأظهر البانر بتخطيط 3 أعمدة الأصلي في المرحلتين (2) و(3) بشكل صحيح؛ لا أخطاء JS في أي مرحلة. رُفع الإصدار: `app.js` v270→v271، `styles.css` v166→v167، `category-slugs.js` v82→v83 (في المواضع الأربعة: `index.html`, `sw.js`, `merchant-app.html`, `customer-app.html`)، و`CACHE` v188→v189 في `sw.js` — وصُحِّح بالمناسبة انحراف قديم كان يجعل `sw.js` يخزّن `category-slugs.js?v=81` بينما الموقع يستخدم إصداراً أحدث فعلياً | `app.js`, `styles.css`, `category-slugs.js`, `index.html`, `sw.js`, `merchant-app.html`, `customer-app.html`, `CLAUDE.md` | نعم (يدوياً بالبحث النصي في الملفات الخمسة؛ `check:versions` غير مربوط بـpackage.json على main) |
| 2026-07-11 | المستخدم طلب التأكد أن مشاركة رابط أي متجر (مثال: `https://www.dukkanci.com.tr/store/bab-touma-restaurant`) تُظهر صورة معاينة (كوفر) للمتجر عند اللصق في واتساب/فيسبوك/تويتر لكل المتاجر | تحقيق كامل عبر Explore agent + استعلام مباشر على جدول `stores` في Supabase (76 صفاً) أثبت أن آلية `og:image` في `api/store.js` (SSR لمسار `/store/:id`) تعمل بشكل صحيح فعلاً لكل المتاجر الـ76 (لا يوجد صف بدون `cover_image`/`image`، وكلها تُحوَّل لرابط مطلق عبر `SITE + img`) — بما فيها `bab-touma-restaurant` (id 33) تحديداً. **الثغرة الحقيقية الوحيدة المكتشفة:** وسم `<meta name="twitter:image">` (ومعه `og:image:type`) لم يكونا ضمن قائمة الاستبدال في `api/store.js`، فبقيا دائماً على قيمة أيقونة الموقع العامة الثابتة في `index.html` (512×512 PNG) حتى لو كانت `og:image` صحيحة لكل متجر — يعني معاينات تويتر/X تحديداً كانت تُظهر شعار دكانجي العام بدل صورة المتجر الفعلية، بينما واتساب/فيسبوك (اللذان يعتمدان `og:image` فقط) كانا سليمين أصلاً | أُضيف استبدال `<meta name="twitter:image">` بنفس قيمة `og:image` المطلقة للمتجر، وأُضيف حساب `og:image:type` ديناميكياً حسب امتداد ملف الصورة الفعلي (png/webp/jpeg) بدل تركه ثابتاً على `image/png`. لم تُمسّ منطق `og:image`/`og:image:secure_url` الأصلي لأنه كان سليماً بالفعل. تحقّق عبر تشغيل `server.js` محلياً (مع `SSR_SHELL_ORIGIN` لتفادي قيد https/host المحلي) وطلب `/api/store?id=bab-touma-restaurant` مباشرة: النتيجة أظهرت `og:image`, `og:image:secure_url`, `twitter:image` جميعها = `https://www.dukkanci.com.tr/assets/photos/babtoma/cover.jpg` و`og:image:type = image/jpeg` بشكل صحيح؛ تأكّد أيضاً أن هذا الرابط يُرجع HTTP 200 فعلياً على الموقع الحي. **ملاحظتان جانبيتان غير معالجتين عمداً (سلوك متوقّع وليس عطلاً):** (1) متجرا id 20 (مرفوض) وid 86 (قيد الانتظار) لا يحصلان على أي OG مخصّص أصلاً بتصميم الكود (`noindex` فقط) — هذا صحيح لأنهما غير معتمدين. (2) إن كان المستخدم لا يزال يرى معاينة قديمة/فارغة لرابط `bab-touma-restaurant` تحديداً في واتساب فالسبب الأرجح هو ذاكرة تخزين مؤقت قديمة لدى واتساب لنفس الرابط (تُحلّ بإعادة فحص الرابط عبر أداة Facebook Sharing Debugger)، وليس عطلاً في الكود | `api/store.js`, `CLAUDE.md` | لا ينطبق (لم يُعدَّل `app.js`/`styles.css`، فقط `api/store.js`) |
| 2026-07-11 | بعد إصلاح شرائح البحث السريع (السطر التالي أسفله)، أكّد المستخدم أن المشكلة الأصلية كانت أيضاً (أو بالأساس) في **النص المكتوب/المتحرك داخل مربع البحث نفسه** (تأثير الكتابة التلقائية «ابحث عن بريان...» عبر `setupHeroSearchTyper()` في `app.js` الذي يحرّك خاصية `input.placeholder`) والذي يظهر أبيض غير مقروء على جهازه الفعلي، رغم أن فحص `getComputedStyle` من داخل متصفح الجلسة (Chromium بدون وضع داكن مفروض) أظهر لوناً رمادياً داكناً `#6b7280` سليماً ظاهرياً | لم تكن هناك أي قاعدة CSS تصرّح `color-scheme: light` لا في `:root` ولا عبر `<meta name="color-scheme">` — هذا يجعل متصفحات الموبايل التي تطبّق «الوضع الداكن التلقائي/القسري» على المواقع التي لا تُصرّح صراحة أنها مصمَّمة للفاتح فقط (شائع في Samsung Internet وAndroid Chrome) تعيد تلوين نص placeholder الأصلي (وأحياناً `-webkit-text-fill-color` بشكل منفصل عن `color`) بلون فاتح/أبيض حتى لو كانت خلفية العنصر نفسها لم تُعكَس (لهذا ظهر الصندوق أبيض والنص أبيض فوقه — عكس تناقض جزئي وليس انعكاساً كاملاً للصفحة) — نفس فئة الخطأ التي عولجت جزئياً في 2026-07-10 بتغيير لون placeholder الثابت لكنها لم تكن كافية لأن ذلك الإصلاح لم يتضمّن تعطيل الوضع الداكن القسري نفسه ولا `-webkit-text-fill-color` | (1) أُضيف `color-scheme: light;` داخل `:root` في `styles.css` بالإضافة إلى `<meta name="color-scheme" content="light">` في `<head>` كل من `index.html`, `customer-app.html`, `merchant-app.html` — هذا يخبر المتصفح صراحة أن الصفحة مصمَّمة للوضع الفاتح فقط فيتجاوز عن تطبيق أي انعكاس/إعادة تلوين تلقائي على عناصرها. (2) قُوِّيت قاعدتا `.hero-search input` و`.hero-search input::placeholder` في `styles.css`: اللون أصبح صريحاً `var(--ink)` (#231f20، أسود تقريباً بدل الرمادي `#6b7280` السابق) مع `!important`، وأُضيفت خاصية `-webkit-text-fill-color: var(--ink)` (تتجاوز أي محاولة WebKit/Samsung Internet لفرض لون نص مختلف عن `color` تحديداً على placeholder) و`caret-color` صريح لثبات شكل المؤشر. تحقّق عبر preview محلي (viewport موبايل 375px): فحص `getComputedStyle(input)` و`getComputedStyle(input, '::placeholder')` أظهر كلاهما `color: rgb(35,31,32)` و`webkitTextFillColor: rgb(35,31,32)` مطابقَين تماماً لـ`--ink`، و`getComputedStyle(document.documentElement).colorScheme` أعاد `"light"` مؤكِّداً تفعيل الخاصية. لم يمكن اختبار وضع Android/Samsung Internet القسري فعلياً من بيئة العمل (لا جهاز فعلي متاح)، لكن `color-scheme: light` هو الحل الموثّق رسمياً (MDN/web.dev) لتعطيل هذا النوع من الانعكاس القسري في كل المتصفحات الحديثة. رُفع الإصدار: `styles.css` v165→v166 (في المواضع الأربعة)، `CACHE` v187→v188 في `sw.js` | `styles.css`, `index.html`, `customer-app.html`, `merchant-app.html`, `sw.js`, `CLAUDE.md` | نعم (يدوياً بالبحث النصي؛ `check:versions` غير مربوط بـpackage.json على main) |
| 2026-07-11 | المستخدم أرفق لقطة شاشة من نسخة الموبايل تُظهر كلمات البحث السريع (chips: «دجاج مشوي»، «رز»، «لحوم»، «حلويات»، «خضار»، «ماركت عربي») تحت مربع البحث بلون أبيض تقريباً وغير مقروء | صنف `.search-chip` في `styles.css` كان مصمَّماً أصلاً لخلفية هيرو داكنة (`color:#fff` + خلفية شبه شفافة `rgba(255,255,255,.08)` + حدّ `rgba(255,255,255,.35)`)، لكن القسم الوحيد الذي يستخدمه فعلياً (`.hero2` في `app.js` عبر `QUICK_SEARCH_CHIPS`) له خلفية فاتحة/كريمية (`var(--cream)` + تدرّجات وردية فاتحة)، فالنص الأبيض على خلفية شبه بيضاء أصبح شبه غير مرئي | غُيِّرت ألوان `.search-chip` لتناسب خلفية `.hero2` الفاتحة: خلفية بيضاء صريحة `#fff`، نص رمادي داكن `#5b5052` (نفس لون `.h2-lead` المستخدم في نفس القسم)، وحدّ بلون العلامة التجارية الخافت `rgba(154,0,0,.16)`؛ حالة hover أصبحت خلفية وردية فاتحة `#fff1ed` ونص أحمر العلامة `#9a0000` بدل التدرّجات البيضاء شبه الشفافة السابقة. تحقّق عبر preview محلي (viewport موبايل 375px): فحص `getComputedStyle` لعنصر `.search-chip` الفعلي على الصفحة الحية أظهر `color: rgb(91,80,82)` و`background: rgb(255,255,255)` و`border: rgba(154,0,0,.16)` مطابقة تماماً للتعديل، ونص الشرائح («دجاج مشوي» إلخ) موجود وقابل للقراءة في محتوى الصفحة. لم تُستخدم `npm run check:versions` (غير مربوط بـ`package.json` على `main`، ملاحظة معروفة) — تحقّق يدوي بالبحث النصي أن `styles.css?v=165` متطابق في `index.html`, `sw.js`, `merchant-app.html`, `customer-app.html`، وأن `CACHE` أصبح `v187` في `sw.js`. رُفع الإصدار: `styles.css` v164→v165 (في المواضع الأربعة)، `CACHE` v186→v187 في `sw.js` | `styles.css`, `index.html`, `sw.js`, `merchant-app.html`, `customer-app.html`, `CLAUDE.md` | نعم (يدوياً بالبحث النصي في الملفات الأربعة؛ `check:versions` غير مربوط بـpackage.json على main) |
| 2026-07-11 | بعد نشر نافذة اقتراح تثبيت التطبيق للموبايل (المهمة السابقة أعلاه)، طلب المستخدم: (1) تصغير صندوق تلك النافذة قليلاً، و(2) تصغير إشعار قبول الكوكيز (البانر السفلي) | (1) نافذة التثبيت كانت تستخدم نفس className `welcome-location-modal` الخاص بنافذة الترحيب/تحديد الموقع (440px)، فلا يمكن تصغيرها وحدها دون التأثير على تلك الأخرى. (2) بانر الكوكيز (`#dk-consent` في `tracking.js`) كان بمقاسات كبيرة نسبياً (padding 18px، عنوان 1.05rem، فقرة 0.9rem، أزرار 11×18px) لم تُصمَّم لتصغير لاحق | (1) فُصلت نافذة التثبيت لclassName مستقل `install-app-modal` (بدل مشاركة `welcome-location-modal`)، وأُضيف بلوك CSS جديد في `styles.css` يقلّص العرض من 440px إلى 360px + padding 26px + أيقونة/عنوان/نص أصغر (58px/19px/13.5px) — دون أي تأثير على نافذة الترحيب بتحديد الموقع التي بقيت 440px كما هي. (2) قُلِّصت كل مقاسات بانر الكوكيز في `tracking.js` (`injectStyles`): padding 18px→13px، فجوة العناصر 18→14px، عنوان 1.05rem→0.92rem، فقرة 0.9rem→0.8rem (line-height 1.7→1.55)، أزرار 11×18px→8×14px (border-radius 12→10px) بخط 0.82rem، لوحة الإعدادات (padding/فجوات الصفوف) و مفتاح التبديل (dk-switch) صُغِّر من 46×26 إلى 42×24 مع تعديل نقطة الإزاحة (translateX) والدائرة الداخلية من 20px إلى 18px تناسقاً. تحقّق عبر preview محلي (viewport موبايل 375px): صندوق التثبيت أصبح عرضه 345px فعلياً (من أصل 375 شاشة، مطابق لـ`min(360px, 100%-30px)`) بدل 345px+ سابقاً المشترك مع نافذة الترحيب، وبانر الكوكيز (بعد مسح كوكي `dukkanci_consent` لإجباره على الظهور) أظهر padding 13px/12px وعنوان 14.72px (0.92rem) وفقرة 12.8px (0.8rem) وأزرار padding 8px/14px — كلها مطابقة للتعديل. لا أخطاء JS في الكونسول. رُفع الإصدار: `app.js` v269→v270، `styles.css` v163→v164 (في index.html وsw.js وأيضاً merchant-app.html وcustomer-app.html اللذين يحمّلان نفس styles.css)، `tracking.js` v6→v7 في index.html فقط (غير مُدرَج أصلاً في مصفوفة APP_SHELL بـsw.js)، و`CACHE` v185→v186 في sw.js | `app.js`, `styles.css`, `tracking.js`, `index.html`, `sw.js`, `merchant-app.html`, `customer-app.html`, `CLAUDE.md` | نعم (يدوياً بالبحث النصي في الملفات الأربعة) |
| 2026-07-11 | المستخدم طلب: عند تصفّح الموقع من الموبايل، إظهار نافذة منبثقة (بوب أب) تقترح تنزيل الموقع كتطبيق لتجربة استخدام أفضل | لم تكن هناك أي مبادرة تلقائية لاقتراح التثبيت على زوار الموبايل — آلية `beforeinstallprompt`/`state.deferredInstall` وزر `data-action="install-app"` كانا موجودين مسبقاً لكن **مدفونين** فقط داخل فوتر الصفحة الرئيسية (زر يدوي يعمل فقط إن ضغطه الزائر بنفسه)، فمعظم زوار الموبايل لا يكتشفون إمكانية التثبيت إطلاقاً | أُضيفت `showInstallAppModal()` (تعيد استخدام تنسيق `.welcome-modal` الموجود مسبقاً بلا أي CSS جديد؛ عنوان "ثبّت تطبيق دكانجي" + زرّي "تثبيت التطبيق الآن" `data-action="install-app"` الموجود أصلاً و"لاحقاً" `data-action="close-modal"` الموجود أصلاً) و`initInstallPrompt()` التي تستدعيها بعد تأخير 3.5 ثانية وفق شروط: عرض الشاشة موبايل فقط (`matchMedia max-width:767px`)، التطبيق ليس مثبَّتاً أصلاً (`isStandaloneDisplay()` مطابقة لمنطق `isMerchantPwaContext` الموجود)، ليس على رابط حسّاس (admin/merchant/checkout)، ولم تُعرض من قبل على هذا الجهاز (`localStorage["dukkanci-install-prompted"]`، مرة واحدة فقط للأبد كنمط `dukkanci-welcomed` الموجود). كما تتحقق عند إطلاقها فعلياً أن لا نافذة منبثقة أخرى مفتوحة حالياً (مثل نافذة الترحيب بتحديد الموقع) فلا تتزاحم الاثنتان. استُدعيت `initInstallPrompt()` بعد `initUserLocation()` مباشرة في تسلسل الإقلاع. تحقّق كامل عبر preview محلي (viewport موبايل 375px): النافذة ظهرت بعد ~3.5 ثانية بالمحتوى الصحيح، زر "لاحقاً" أغلقها فعلياً عبر معالج النقر الحقيقي بالتفويض، إعادة تحميل الصفحة بعدها لم تُعِد إظهارها (العلَم محفوظ)، وعلى عرض سطح مكتب (885px) لم تظهر إطلاقاً ولم يُحفظ العلَم. لم تُستخدم `npm run check:versions` لأن السكربت غير مربوط بـ`package.json` على `main` حالياً (نفس الملاحظة من سجل سابق) — تحقّق يدوي بالبحث النصي أن `app.js?v=269` متطابق في index.html وsw.js وأن `CACHE` أصبح `v185` | `app.js`, `index.html`, `sw.js`, `CLAUDE.md` | نعم (يدوياً، تحقّق بالبحث النصي — `check:versions` غير مربوط بـpackage.json على main) |
| 2026-07-11 | المستخدم طلب رابط مباشر لتطبيق الزبائن ليُرسله لمن يسأل عنه — تبيّن أن تطبيق الزبون العام (خلافاً لتطبيق التاجر) لا يملك أي رابط/صفحة تنزيل مستقلة، فقط زر "تثبيت التطبيق" JS-driven مدفون داخل فوتر الصفحة الرئيسية، غير قابل للمشاركة كرابط منفصل | لم تكن هناك صفحة هبوط مخصّصة لتطبيق الزبون العام مطلقاً — فقط `manifest.json` + زر `data-action="install-app"` يعمل فقط إن كان الزائر أصلاً على الصفحة الرئيسية وتلقّى المتصفح حدث `beforeinstallprompt` | أُنشئت `customer-app.html` (نسخة مطابقة تماماً لبنية `merchant-app.html` من الجلسة السابقة، بنفس أزرار "تثبيت التطبيق على الشاشة الرئيسية" + تعليمات آيفون اليدوية) لكن **بمحتوى وقيم مختلفة**: تستخدم `manifest.json` العام الموجود مسبقاً (وليس مانيفست جديد، فالتطبيق العام ليس بحاجة هوية PWA منفصلة عن الموقع نفسه)، وزر رئيسي "تصفّح المتاجر الآن" (→ `/`) بدل رابط لوحة تحكم. أُضيف رابط `/app` في `vercel.json` و`server.js` (نفس نمط `/merchant-app`↔`merchant-app.html`). أُضيف أيضاً سطر رابط نصي قابل للمشاركة "رابط للمشاركة: dukkanci.com.tr/app" أسفل زر التثبيت الحالي في فوتر `index.html` (لم يُغيَّر سلوك الزر الأصلي `data-action="install-app"` تفادياً لأي تراجع) مع نمط CSS جديد `.footer-apps__share-link` في `styles.css`. تحقّق عبر preview محلي: `/app` يعرض الصفحة الصحيحة بعنوان ومحتوى مخصّص للزبون، الزر الرئيسي يوجّه لـ `/`، ورابط المشاركة بالفوتر يشير لـ `/app` بشكل صحيح. رُفع رقم الإصدار v163→ لـ styles.css (لم يكن هناك تعديل على app.js هذه المرة) وv183→v184 لثابت CACHE في sw.js | `customer-app.html` (جديد), `vercel.json`, `server.js`, `index.html`, `styles.css`, `sw.js`, `merchant-app.html` (تحديث مرجع رقم إصدار styles.css فقط), `CLAUDE.md` | نعم (يدوياً) |
| 2026-07-10 | تاجر ثبّت تطبيق دكانجي للتجار (PWA) على موبايله لكن واجهته عند الدخول بدت مطابقة لواجهة تطبيق الزبون العادي بدل شاشة دخول مخصّصة برقم الموبايل وكلمة المرور | `renderMerchant()` كان يتخطّى شاشة `merchantLogin()` بالكامل بمجرد وجود أي جلسة Supabase قائمة (`state.user`) — بغضّ النظر عن كونها جلسة زبون عادي بقيت محفوظة في نفس تخزين المتصفح على نفس الجهاز (localStorage/cookies مشتركة لأن تطبيق التاجر وتطبيق الزبون على نفس النطاق)، فيقفز مباشرة لمحاولة `resolveMerchantStores()` التي تفشل لجلسة زبون فعلياً وتنتهي بشاشة "جارٍ التحقق…" أو "لا يوجد متجر مرتبط بحسابك" — لا تشبه لوحة تاجر مخصّصة إطلاقاً | أُضيفت `isMerchantPwaContext()` (تتحقق من علامة `localStorage["dk-merchant-app"]` التي يضبطها `merchant-app.html` عند أي زيارة، **و**كبديل احتياطي للتطبيقات المثبَّتة قبل هذا الإصلاح: أي نافذة `display-mode: standalone`/`navigator.standalone` تفتح على `/merchant` — فلا حاجة لإعادة تثبيت التطبيق) و`hasMerchantGoogleIntent()` (علامة `localStorage` تُضبط فقط عند نقر التاجر الصريح على زر "دخول بحساب بريد إلكتروني (Google)"، ضرورية لأن `signInWithOAuth` يُعيد تحميل الصفحة بالكامل فيصفّر أي متغيّر حالة عابر). عدَّلت شرط `renderMerchant()`: داخل سياق تطبيق التاجر تحديداً (`isMerchantPwaContext()`)، إن كان وضع الدخول لا يزال الافتراضي `"admin"` ولم يختر التاجر صراحة Google/بريد إلكتروني هذه الزيارة، تُعرض شاشة `merchantLogin()` دائماً — حتى لو وُجدت جلسة زبون سابقة — بدل القفز الصامت لمحاولة الحل عبرها؛ سلوك الموقع العادي (رابط "لوحة المتجر" بالفوتر/الهيدر خارج تطبيق التاجر) **لم يتغيّر** لتفادي أي تراجع لتجار يستخدمون تسجيل الدخول بـGoogle/البريد بشكل معتاد. كما غُيِّر عنوان شاشة الدخول بوضع `"admin"` من "دخول إدارة دكانجي" إلى "أهلاً بكم في شاشة التحكم بالمتاجر" بناءً على طلب المستخدم الصريح للنص. تحقّق عبر preview: محاكاة `state.user` (جلسة زبون وهمية) + علامة `dk-merchant-app` مضبوطة → عرضت شاشة الترحيب برقم الموبايل/كلمة المرور بدل القفز للحل التلقائي (مطابقة). نفس المحاكاة بدون العلامة (سياق الموقع العادي) → السلوك القديم بقي كما هو (تراجع آمن). محاكاة `display-mode: standalone` بدون علامة `localStorage` → `isMerchantPwaContext()` أعادت `true` (يُصلح التطبيق المثبَّت مسبقاً فوراً دون إعادة تثبيت). رُفع رقم الإصدار v267→v268 لـ app.js وv182→v183 لثابت CACHE في sw.js (سكربت `check:versions` غير موجود ضمن `package.json` على هذا الفرع فتحقّقت يدوياً من تطابق الأرقام الثلاثة) | `app.js`, `merchant-app.html`, `index.html`, `sw.js`, `CLAUDE.md` | نعم (يدوياً) — **مدفوع لفرع قصير `merchant-app-login-fix` ودُمج فوراً في `main` عبر fast-forward بناءً على طلب المستخدم الصريح «ارفع»**، حي على الموقع |
| 2026-07-10 | طلب المستخدم تطبيق موبايل مخصص لأصحاب المتاجر فقط (بدون ميزات الزبون/الإدارة العليا) لتسهيل متابعة الطلبات بصوت تنبيه وإدارة المنتجات من الموبايل بدون كمبيوتر، مع رابط تنزيل يُشارَك عبر فوتر الموقع | فحصنا أولاً غلاف Capacitor العام الموجود مسبقاً (`android/`, `ios/`, `capacitor.config.json`) فتبيّن أنه غير مخصص للتاجر (يفتح كامل الموقع من `#home`) وإشعاراته الأصلية (FCM/APNs) غير مفعّلة ولا يمكن بناء .apk/.ipa فعلي من هذه البيئة (لا Android SDK ولا Mac). بالمقابل، لوحة التاجر (`renderMerchant`) ونظام Web Push (VAPID) وصوت التنبيه المولَّد بـ Web Audio (`merchantOrderTick`/`newOrderChime`) **موجودة ومفعّلة بالكامل فعلاً** ولا تحتاج بناء من الصفر — طلب المستخدم اختيار مسار PWA (تطبيق ويب مثبَّت) بدل التطبيق الأصلي عبر متاجر التطبيقات | أُنشئ `manifest-merchant.json` (هوية PWA مستقلة: `start_url:"/merchant"`, اسم "دكانجي للتجار") وصفحة تنزيل خفيفة `merchant-app.html` (تصميم مطابق لبطاقة `merchant-auth__card` الموجودة، بدون تحميل `app.js` أو حزم بيانات المتاجر لسرعة أعلى) تعرض مزايا التطبيق وزر "فتح لوحة التاجر" (→ `/merchant`، يدخل مباشرة لشاشة تسجيل دخول التاجر بفضل `dashboard-view` الذي يخفي هيدر/فوتر/تنقل الزبون تلقائياً) وزر تثبيت يلتقط `beforeinstallprompt` مع تعليمات يدوية لآيفون (Safari لا يدعم التثبيت التلقائي). أُضيف رابط `/merchant-app` في `vercel.json` (rewrite بنفس نمط `/merchants`↔`merchants.html`) **و**`server.js` (جدول `cleanUrls` المحلي كان ناقصاً هذا المسار فكان يُعيد `index.html` بدل الصفحة الجديدة محلياً — لوحظ ذلك أثناء الاختبار عبر preview وأُصلح). أُضيف رابط "تطبيق دكانجي للتجار" في قسم "تطبيقاتنا" بفوتر `index.html` بجانب زر تثبيت تطبيق الزبون الموجود مسبقاً. **حادثة أثناء التنفيذ:** منتصف الجلسة تغيّر الفرع المفعّل من `supermarket-baseline-categories` إلى `main` بفعل عملية خارجية (جلسة Claude أخرى تعمل بالتوازي على نفس المستودع على الأرجح)، مما تسبب بقراءة/تعديل عن طريق الخطأ لنسخة `index.html`/`vercel.json` الأقدم على `main`؛ تم اكتشاف ذلك عبر `git status`/`git reflog` قبل أي ضرر (لا فقدان commits، والتعديل غير المحفوظ حُفظ تلقائياً في `git stash`)، والعودة لـ `supermarket-baseline-categories` وإعادة التنفيذ هناك بشكل صحيح. تحقّق كامل عبر preview محلي: `/merchant-app` يعرض الصفحة الصحيحة، `beforeinstallprompt` يُفعِّل زر التثبيت تلقائياً، النقر على "فتح لوحة التاجر" ينقل مباشرة لشاشة "دخول إدارة دكانجي" بدون أي واجهة زبون، ورابط الفوتر يظهر بشكل صحيح | `manifest-merchant.json` (جديد), `merchant-app.html` (جديد), `vercel.json`, `server.js`, `index.html`, `CLAUDE.md` | لا ينطبق (لم يُعدَّل `app.js`/`styles.css`) — **مدفوع (7b38af0) لفرع `supermarket-baseline-categories` البعيد** بناءً على طلب المستخدم «اعمل commit وادفعها» (الفرع البعيد كان محذوفاً فأُعيد إنشاؤه بالدفع) — **لم يُدمج بعد في `main`**، فلن يظهر على الموقع الحي حتى يُدمج الفرع |
| 2026-07-10 | المستخدم طلب إضافة حقل «تاريخ الطلب» و«ساعة الطلب» في جدول الطلبات بلوحة التاجر ولوحة إدارة دكانجي العليا (أرفق لقطة شاشة من «إدارة الطلبات» في لوحة التاجر تُظهر عمود «الوقت» يعرض «الآن» لطلب قديم) | عمود «الوقت» في `renderOrdersTable()` (المشتركة بين لوحتي التاجر والإدارة عبر `context`) كان يعرض حقل `order.time` الذي يُخزَّن كنص ثابت `"الآن"` وقت إنشاء الطلب فقط (`app.js` عند بناء `newOrder`) ولا يتحدّث أبداً — بينما التاريخ/الوقت الحقيقي (`order.createdAt`، مبني من `dd.createdAt \|\| r.created_at`) كان موجوداً في البيانات ولم يُستخدم في الجدول إطلاقاً | أُضيفت دالتا تنسيق `formatOrderDateOnly(iso)` و`formatOrderTimeOnly(iso)` (بجانب `formatOrderDate` الموجودة مسبقاً) في `app.js`، واستُبدل عمود «الوقت» الوحيد في `renderOrdersTable()` بعمودين «تاريخ الطلب» و«ساعة الطلب» مبنيين من `order.createdAt` (مع fallback لـ `order.time`/"—" عند غياب `createdAt`). كما حُدِّث `exportCsv("orders")` ليضيف نفس العمودين لملف تصدير الطلبات. تحقّق بصري عبر preview: تسجيل دخول admin محلي (`localhost:4173/#admin`، كلمة مرور dev) وفتح تبويب «الطلبات» — نفس الطلب من لقطة شاشة المستخدم (`DK-644234315`, Hamada hilal) ظهر بعمودين «١٠ يوليو ٢٠٢٦» و«٠٣:٤٣ ص» بدل «الآن» الثابتة. لوحة التاجر تستخدم نفس الدالة `renderOrdersTable()` فتستفيد من الإصلاح تلقائياً دون تعديل إضافي. رُفع رقم الإصدار v266→v267 لـ app.js وv181→v182 لثابت CACHE في sw.js، وتشغيل `npm run check:versions` (نجح ✅). دُفع (41856ae5) لفرع `supermarket-baseline-categories` البعيد بناءً على طلب المستخدم «commit and push it» — **لم يُدمج بعد في `main`**، فلن يظهر على الموقع الحي (Vercel ينشر من `main` فقط) حتى يُدمج الفرع | `app.js`, `index.html`, `sw.js`, `CLAUDE.md` | نعم — `npm run check:versions` ناجح؛ **مدفوع لفرع `supermarket-baseline-categories`، غير مدموج بـ main بعد** |
| 2026-07-10 | تاجر أبلغ (عبر لقطة شاشة من لوحة متجره — صفحة «بيانات الحساب البنكي/طرق الدفع/التوصيل التلقائي») أن حفظ التعديلات يفشل برسالة «تعذّر حفظ بيانات المتجر، تحقّق من الاتصال» رغم أن اتصاله بالإنترنت سليم | جلسة التاجر (`x-merchant-token`، تسجيل دخول برقم هاتف+كلمة مرور) تنتهي صلاحيتها بعد 12 ساعة من طرف الخادم (`signMerchantToken` في `api/notify-order.js`)، لكن الواجهة الأمامية لم تكن تتحقق من هذا إطلاقاً — فقط من غياب التوكن كلياً (جلسات قديمة قبل نظام التوكن). فبعد 12 ساعة يبقى التاجر «مسجّلاً دخول» ظاهرياً بينما كل طلب حفظ يفشل بـ403 صامتاً، وتظهر رسالة عامة مضلِّلة تلوم الاتصال بدل تسجيل الدخول من جديد. تأكدنا عبر Vercel MCP (`get_runtime_logs`) من نمط 403 متكرر على `/api/notify-order` كل ~15 ثانية (استطلاع الطلبات الدوري `_merchantOrderWatch`) بالتزامن مع محاولات POST متقطعة (محاولات الحفظ الفعلية للتاجر) في نفس الفترة الزمنية التي أبلغ فيها التاجر عن المشكلة | أضيفت `handleMerchantSessionExpired()` في `app.js` تُفرّغ `state.merchantPwAuth` وتُعيد التاجر لشاشة الدخول برسالة صريحة «انتهت صلاحية الجلسة، الرجاء تسجيل الدخول من جديد» بدل الصمت. `pushStoreCloud()` غُيِّر عائدها من boolean إلى `{ok, status, error}` (مطابقةً لنمط `pushProductCloud` الموجود مسبقاً) وتستدعي هذا المعالج تلقائياً عند 401/403 مع توكن تاجر؛ نفس المعالج أُضيف في مسار التاجر داخل `loadOrdersFromSupabase()` (الاستطلاع الدوري) ليكتشف انتهاء الجلسة خلال ثوانٍ بدل أن تبقى صامتة لساعات/أيام. حُدِّثت نقطتا استدعاء `pushStoreCloud` الأخرى (نموذج بيانات المتجر، وتبديل «مميّز» من لوحة الإدارة) لتتعامل مع الشكل الجديد للعائد. اختُبر عبر محاكاة توكن تاجر مزوَّر في المتصفح المحلي — تأكّدنا أن `pushStoreCloud` أعادت `{ok:false,status:403}` وأن الواجهة قفزت تلقائياً لشاشة تسجيل الدخول دون أخطاء JS. رُفع رقم الإصدار v265→v266 لـ app.js وv180→v181 لثابت CACHE في sw.js، وتشغيل `npm run check:versions` (نجح ✅). دُفع مباشرة لـ main (578b7cfe) بناءً على طلب المستخدم الصريح «ارفع هذا الإصلاح فقط لـ main» — استُثنيت عمداً كل التعديلات الأخرى غير المرتبطة الموجودة على نفس الفرع وقتها (package.json/package-lock.json من `npm install` غير مرتبط، وملفات/مجلدات غير متتبَّعة مثل `.agents/`, `android/`, `ios/`, `catalog/`, `scripts/`, ملفات docx/md بحثية) فلم تُدفع معها | `app.js`, `index.html`, `sw.js`, `CLAUDE.md` | نعم — `npm run check:versions` ناجح؛ **مدفوع لـ main** (578b7cfe، فوق 1680e396 حذف حلاب 1881 الذي كان الالتزام الوحيد السابق غير المدفوع على نفس الفرع) |
| 2026-07-10 | متجر حلاب 1881 (id 49، مُغلَق) كان لا يزال يظهر فعلياً على الموقع رغم حذف صفه من Supabase في تدقيق العناوين السابق (نفس اليوم) | حذف صف Supabase وحده لا يكفي لهذا المتجر تحديداً: عكس أغلب `*-data.js` "الميتة" في قسم 2، ملف `hallab-data.js` كان **مُحمَّلاً فعلياً** عبر `<script>` في `index.html` ومُدرَجاً في `sw.js` (APP_SHELL)، ومُدمَجاً يدوياً في `app.js` عبر `stores.push(hallabStore, ...)`, `products.push(...hallabProducts)`, `...hallabDeliverySettings` — تحقّقت أيضاً أن Supabase (كل الجداول: stores/products/orders/store_credentials/store_reviews/store_staff/store_users/merchant_notifications/whop_subscriptions/site_settings) خالٍ تماماً من أي أثر لـ id 49 أو "حلاب" أو "1881" | حذف `hallab-data.js` وكامل مجلد `assets/photos/hallab/` (240 ملف) من git، وإزالة الأسطر الثلاثة المرتبطة به من `app.js`، وإزالة وسم `<script>` من `index.html`، وإزالة الإدخال من مصفوفة `APP_SHELL` في `sw.js`؛ رفع رقم الإصدار (v264→v265 لـ app.js، وv179→v180 لثابت CACHE في sw.js) وتشغيل `npm run check:versions` (نجح ✅). أُضيف بند تحذيري جديد في قسم 6 لتفادي تكرار هذا النمط مستقبلاً مع أي متجر مُدمَج يدوياً بنفس الطريقة. **ملاحظة:** نُسخة عمل منفصلة `.worktree-fbads-2/` لا تزال تحتوي على نفس الملفات القديمة لحلاب 1881 — لم تُلمس لأنها متعلقة بعمل fbads منفصل غير مدموج | `app.js`, `index.html`, `sw.js`, حذف `hallab-data.js` و`assets/photos/hallab/**`, `CLAUDE.md` | نعم — `npm run check:versions` ناجح |
| 2026-07-10 | مزامنة حقل `hours` لكل متاجر دكانجي (76 متجراً نشطاً) مع ساعات العمل الفعلية المسجَّلة في غوغل مابس، بدل نصوص عامة/تخمينية | لا يوجد Google Places API key محلي متاح مباشرة؛ `api/maps-key.js` يُخفي المفتاح خلف endpoint حي فقط | جلب مفتاح Maps الحي عبر `https://www.dukkanci.com.tr/api/maps-key`، ثم مطابقة كل متجر ببحث Places (place_id مباشر من `map_url` عند توفره، وإلا Nearby/Text Search بالاسم اللاتيني + الإحداثيات)، وجلب `opening_hours.weekday_text` عبر Place Details، وتحويلها لعبارة عربية واحدة موحّدة الأسلوب، وتحديث `stores.hours` عبر Supabase MCP لـ 72 متجراً. **4 متاجر استُثنيت عمداً (لم تُعدَّل ساعاتها):** id 12 (حلويات زيتونة - بهشة ايفلر: لا يوجد فرع مطابق قريب، أقرب نتيجة "Zaitoune Factory" تبعد 5.1 كم)، id 22 (القاضي ماركت - اسنيورت: أقرب نتيجة "Çağrı Market Beylikdüzü" اسم مختلف تماماً، رُفضت)، id 30 (الأغر شاورما - شيرين ايفلر: الفرع مطابق لكنه بلا ساعات عمل منشورة على غوغل)، id 85 (قرقوشة فرفوشة: لا يوجد `map_url` أصلاً — مطبخ منزلي). **ملاحظة شكّ:** id 51 (مطعم رودي) ساعات غوغل الرسمية معكوسة منطقياً ("12:00 ص – 12:00 م" أي منتصف الليل حتى الظهر لمطعم) — رُحّلت كما هي بالحرف تنفيذاً لأمر "مطابقة 100%"، لكنها تستحق تنبيه صاحب المتجر لتصحيحها في غوغل مابس إن كانت خطأ إدخال. سكربتات المطابقة (Node + Places API) محفوظة في scratchpad الجلسة وليست جزءاً من المستودع | لا شيء في الكود (تعديل بيانات Supabase فقط عبر `execute_sql`)، `CLAUDE.md` | لا ينطبق (لا تعديل على app.js/styles.css) |
| 2026-07-10 | نص البحث المتحرك (hero-search) في نسخة الموبايل يظهر بلون فاتح شبه أبيض وغير مقروء (لاحظه المستخدم عبر لقطة شاشة من Samsung Internet) | `.hero-search input::placeholder` كان بلون `#9ca49f` فاتح جداً بدون `::-webkit-input-placeholder` أو `opacity:1` صريحة، فبعض متصفحات الموبايل تعرضه شبه أبيض | تغميق اللون إلى `#6b7280` + إضافة selector مخصص لـ webkit + `opacity:1`؛ بالمناسبة رُفع رقم إصدار styles.css إلى v=162 وصُحّح انحراف app.js القديم (v176 في sw.js مقابل v264 في index.html، المذكور في قسم 4) إلى v264 في الموضعين | `styles.css`, `index.html`, `sw.js` | نعم — تحقّق `npm run check:versions` ناجح؛ تم الدفع مباشرة لـ main (11148cd0) بناءً على طلب المستخدم الصريح «ارفع» |
| 2026-07-10 | فحص 15 نسخة عمل (worktree) و52 فرعاً محلياً و45 فرعاً بعيداً لتحديد الآمن للحذف | تراكم فروع/نُسخ عمل قديمة دون تنظيف بعد كل مهمة (راجع قسم 3) | تأكيد أن كل الـ15 نسخة عمل مفقودة فعلياً من القرص (`gitdir file points to non-existent location`) فالحذف آمن؛ تصنيف 11 فرعاً محلياً و10 فروع بعيدة كمدموجة فعلاً في main (آمنة للحذف)؛ توثيق 39 فرعاً غير مدموج يحتاج مراجعة يدوية (قائمة كاملة في `scripts/git-cleanup.sh`) بدل حذفها عشوائياً. **ملاحظة مهمة:** محاولة تنفيذ الحذف الفعلي من بيئة فحص عن بُعد فشلت بخطأ صلاحيات (`Operation not permitted`) عند لمس `.git/worktrees/*`؛ كما لوحظ أن HEAD لفرع العمل الحالي تغيّر من `ea3d2117` إلى `bef602a1` أثناء الفحص نفسه — أي أن عملاً حيّاً كان يُكتب على نفس الفرع في تلك اللحظة. **لذلك: نفّذ `scripts/git-cleanup.sh` من جهازك مباشرة (طرفية محلية)، وليس عبر جلسة عن بُعد، وتأكد أولاً أن لا أحد يعمل على الفرع في تلك اللحظة.** | لا شيء بعد (السكربت لم يُنفَّذ محلياً بعد) | `CLAUDE.md`, `scripts/git-cleanup.sh` |
| 2026-07-10 | دراسة أولية شاملة لأسباب تكرار الأخطاء وضياع الإصلاحات | تشتت فروع/نُسخ Git، تعدد مواضع رقم الإصدار، غياب ذاكرة مشروع ثابتة، ملف app.js أحادي ضخم، مصدرا بيانات متعارضان، غياب .gitattributes، غياب اختبارات | إنشاء هذا الملف (CLAUDE.md) + `.gitattributes` + سكربت `scripts/check-versions.js` وربطه بـ `npm run check:versions` | `CLAUDE.md`, `.gitattributes`, `scripts/check-versions.js`, `package.json` | لا ينطبق (لا تعديل على app.js/styles.css) |

