<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Networking (Supabase + the existing /api/notify-order backend). -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- WHILE-IN-USE location only, requested from LocationPickerScreen when
         the customer explicitly taps "استخدام موقعي الحالي" — spec section 9
         explicitly bans ACCESS_BACKGROUND_LOCATION for this app; never add it. -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!-- Order-status push notifications (Android 13+ requires this explicit
         runtime permission; requested after the customer places their first
         order per spec section 19, not at app launch). -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:label="دكانجي"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <!-- Android Maps SDK key — see README "Required manual setup": create
             a SEPARATE Android-restricted key (package name + SHA-1
             fingerprints) in Google Cloud Console, do not reuse the website's
             HTTP-referrer-restricted key. -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="${MAPS_API_KEY}" />

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>

            <!-- Android App Links (spec section 34) — verified once
                 https://www.dukkanci.com.tr/.well-known/assetlinks.json is
                 published with this app's release SHA-256 fingerprint.
                 android:autoVerify requires that file to exist BEFORE this
                 ships, or Android silently falls back to a disambiguation
                 dialog instead of opening the app directly. -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="https" android:host="www.dukkanci.com.tr" android:pathPrefix="/store" />
                <data android:scheme="https" android:host="www.dukkanci.com.tr" android:pathPrefix="/product" />
                <data android:scheme="https" android:host="www.dukkanci.com.tr" android:pathPrefix="/order" />
            </intent-filter>
            <!-- Internal dukkanci:// scheme as a fallback deep link. -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="dukkanci" />
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
